# Dependencies — rebuilt inside container
node_modules/
**/node_modules/
npm-debug.log*

# Secrets — never bake into image
.env
cwn-drive-key.json
client_secret_*.json

# Runtime state — bind-mounted at run time, not baked in
output/
tmp/
logs/
data/jobs.json

# Next.js build artifacts (frontend is a separate Render service)
app/.next/
app/out/

# Large local asset directories not needed in API container
The_Wrangler_Video_frames/
Twitch Brand/

# Dev / test artifacts
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Python virtualenv (VectCutAPI)
VectCutAPI/venv*/
VectCutAPI/__pycache__/
**/__pycache__/
**/*.py[cod]

# Agent/IDE state
.cline/
.clinerules/
.aider*

# Media assets (sourced from Drive at runtime)
*.mp4
*.mov
*.avi
*.jpeg
*.jpg
*.png
*.gif

# Docs and planning
docs/
*.pdf
*.md

# Git
.git/
.gitignore

# Test infrastructure (not needed at runtime)
test/
qa/
