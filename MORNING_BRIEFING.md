# AuraFlux — Morning Briefing

> Auto-updated by Cursor / Aider sessions. Most recent entry at top.

---

## 2026-04-30

### CPD-6: Wan2.1 Frame Count Test — Status: BLOCKED (no GPU)

**Test objective:** Validate `generateWanVideo()` with longer clips and vertical resolution before committing to these frame counts in production.

**Pod:** `5a8c7dkr95eohw` (RTX 3090, 24 GB VRAM)  
**Pod status:** EXITED — host machine had no free GPU at test time. Start manually in RunPod dashboard when available.

**To run when pod is up:**
```bash
node scripts/test_wan_frames.js
```
Or a single case:
```bash
node scripts/test_wan_frames.js --case 2 --prompt "Your prompt here"
```

**Test matrix:**

| Case | Resolution | Frames | Duration | Expected | Notes |
|---|---|---|---|---|---|
| 1 | 832×480 (16:9) | 25 | 1.5 s | ✅ Works | Baseline — confirmed working |
| 2 | 832×480 (16:9) | 49 | 3 s | Likely OK | 2× memory vs baseline — should fit 24 GB |
| 3 | 832×480 (16:9) | 81 | 5 s | Unknown | Near 1.3B model limit — may OOM |
| 4 | 720×1280 (9:16) | 49 | 3 s | Unknown | 2.3× pixels vs baseline — watch for OOM |

**Known 1.3B model limits (from WAN repo docs):**
- Tested up to 81 frames at 480p on 24 GB cards — theoretically fits
- Vertical 9:16 at 720×1280 is untested; may require reducing numFrames to 25 first

**Results:** *(fill in after test run)*
- Case 2 (3 s 16:9): ?
- Case 3 (5 s 16:9): ?
- Case 4 (3 s 9:16): ?

**Report path:** `docs/reports/wan_frame_test_{ts}.md` (auto-generated by test script)

---

### CPD-5: /dashboard/generate page — Status: DONE

Built the Next.js `/dashboard/generate` page:
- `app/src/app/dashboard/generate/page.tsx` — prompt input, frame/resolution presets (1.5s, 3s, 5s, 9:16 3s), StatusPoller (5 s interval), VideoPreview with download link
- `app/src/lib/api.ts` — added `generateVideo()` and `pollVideoStatus()` typed functions
- `app/src/components/layout/sidebar.tsx` — added "Generate" nav item between Jobs and AI Concierge
- Backend `POST /api/generate-video` was already wired (`lib/routes/video.js` → `server.js`)
