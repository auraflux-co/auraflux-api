# CPD-990 — pip constraints for the EchoMimicV3 worker image.
# Validated pinset from the CPD-881 spike (Confluence 31555587, gotchas section).
# Applied with `pip install -c constraints.txt` so upstream requirements.txt can
# never move these — torch 2.12 breaks torchaudio ABI; transformers/diffusers
# pins are era-matched to torch 2.4.
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
transformers==4.53.3
diffusers==0.34.0
numpy<2
