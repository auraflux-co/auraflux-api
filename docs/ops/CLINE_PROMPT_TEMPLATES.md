# Cline Session Prompt Templates

**How to use:** Copy the prompt for the agent, paste it as the opening message.
These are regenerated by Claude Code at the start of each session based on current branch state.
Last updated: 2026-04-15

**REQUIRED final output (every agent, every task):**
When done, your last message must include: branch name, commit hash(es), what was done (one line per task), `node -c` pass confirmation. See `docs/ops/COMMIT_CHECKLIST.md` for the exact format. Without this, Claude Code cannot review and merge your work.

---

## Cline-A — cline-a/shorts-qa-errors-ffmpeg

```
SHELL RULE: Every grep/find/rg/ls must end with || true — no exceptions. Exit code 1 from a bare search causes an infinite retry loop.

You are Cline-A working on branch cline-a/shorts-qa-errors-ffmpeg.

WHY THIS BRANCH EXISTS:
Two pipeline gaps that slow every production run:
1. Bobby G hard-cuts on his last word "Appreciate you!" — the video ends before his pose settles. Needs a 0.75s freeze-hold on the final frame.
2. Assembly uses software libx264 (~5 min on M4 Pro). Apple VideoToolbox hardware encoder cuts that to ~1 min with identical output quality.

WHERE TO FIND CONTEXT:
- BRANCH_NOTES.md — exact grep commands, code snippets, commit messages
- docs/handoffs/CLINE_HANDOFF_FFMPEG_PERFORMANCE.md — VideoToolbox full spec
- docs/handoffs/CLINE_HANDOFF_WAVE_0_CLEANUP.md → Gap #45 — freeze-hold spec

WHAT TO DO (2 tasks, in order):

TASK 1 — Outro freeze-hold
File: lib/assembly.js
Find the OUTRO segment in the per-segment loop, add a tpad FFmpeg filter after the existing burns. Long-form only — skip if isShortForm.
Filter: tpad=stop_mode=clone:stop_duration=0.75
One commit: fix(assembly): freeze-hold last frame of outro 0.75s (Gap #45)

TASK 2 — VideoToolbox encoder
File: lib/assembly.js (ffmpegEncodeArgs function)
Make it platform-aware: h264_videotoolbox on darwin, libx264 ultrafast on linux.
One commit: feat(ffmpeg): VideoToolbox on macOS, libx264 fallback on Linux

RULES:
- Never read lib/assembly.js in full — grep -n first, read ±50 lines around the match
- node -c lib/assembly.js must pass before every commit
- Update STATUS.md → Last Agent Action table in every commit
- ⛔ NEVER commit to main. Run git branch --show-current before every commit — it must say cline-a/shorts-qa-errors-ffmpeg. If it says main, stop and run git checkout cline-a/shorts-qa-errors-ffmpeg first.
- When done: your final message must include branch name (cline-a/shorts-qa-errors-ffmpeg), commit hash(es), what was done, node -c confirmation

Start: git checkout cline-a/shorts-qa-errors-ffmpeg
```

---

## Cline-B — cline-b/gate3-autopublish-fix

```
SHELL RULE: Every grep/find/rg/ls must end with || true — no exceptions. Exit code 1 from a bare search causes an infinite retry loop.

You are Cline-B working on branch cline-b/gate3-autopublish-fix.

WHY THIS BRANCH EXISTS:
Every video assembled on April 15 completed FFmpeg successfully but never uploaded to Drive or published to YouTube. Root cause: when Gate 3 QA throws an error (Gemini timeout, upload error), the fallback sets passed=false, which silently blocks Gate 6 auto-publish. A Gemini API error is not a QA fail — the video should still go to YouTube as private so Rob can review it.

WHERE TO FIND CONTEXT:
- BRANCH_NOTES.md — exact line numbers, before/after code, commit message
- docs/handoffs/CLINE_HANDOFF_GATE3_AUTOPUBLISH_FIX.md — full diagnosis

WHAT TO DO (2 tasks, in order):

TASK 1 — Fix Gate 3 error fallback (server.js ~line 5251)
Find the else block after MAX_QA_RETRIES that sets: qaResult = { score: 70, outcome: 'manual_review', passed: false, ... }
Change outcome to 'pass', passed to true, add logError() call so the error is logged to errors.jsonl.
One commit: fix(gate3): error fallback sets passed=true + logError (unblocks Gate 6)

TASK 2 — Move finalizeJobMetrics() to after Gate 3 (server.js ~line 5164)
Remove the existing finalizeJobMetrics(asmId) call before Gate 3.
Add it back after the Gate 3 block completes, right before the Drive upload step (~line 5275).
Also add Gate 3 timing: StageTimer for Gate 3 with score, outcome, retryAttempts — addStageMetrics before finalizeJobMetrics.
One commit: fix(metrics): move finalizeJobMetrics after Gate 3 so QA stage appears in run_metrics

RULES:
- Never read server.js in full — grep -n first, read ±50 lines around the match
- node -c server.js must pass before every commit
- Update STATUS.md → Last Agent Action table in every commit
- ⛔ NEVER commit to main. Run git branch --show-current before every commit — it must say cline-b/gate3-autopublish-fix. If it says main, stop and run git checkout cline-b/gate3-autopublish-fix first.
- When done: your final message must include branch name (cline-b/gate3-autopublish-fix), commit hash(es), what was done, node -c confirmation

Start: git checkout -b cline-b/gate3-autopublish-fix
```

---

## Cline-B — cline-b/wave0-remaining

```
SHELL RULE: Every grep/find/rg/ls must end with || true — no exceptions. Exit code 1 from a bare search causes an infinite retry loop.

You are Cline-B working on branch cline-b/wave0-remaining.
You are running Haiku — keep tasks small and focused. One file at a time. If logic spans more than 2 files, stop and flag it.

WHY THIS BRANCH EXISTS:
Three gaps in lib/publish.js:
1. News and NBA publish copy is too generic — Rob uses ChatGPT instead because the output doesn't reference actual story headlines or game data. Fix: inject items[] data into the prompts.
2. After publishing, we only log "Published to youtube" — no confirmation that the video actually landed on each platform. Fix: fire-and-forget poll of Upload-Post status endpoint, log per-platform results.

WHERE TO FIND CONTEXT:
- BRANCH_NOTES.md — exact grep commands, what to change, commit messages
- docs/handoffs/CLINE_HANDOFF_WAVE_0_CLEANUP.md → Gap #13 (News copy), Gap #34 (NBA copy), Gap #17 (Upload-Post polling)

WHAT TO DO (3 tasks, in order):

TASK 1 — News publish copy quality (Gap #13)
File: lib/publish.js — find the news: prompt block
Inject actual story headlines from the items[] array that's already in scope.
One commit: fix(publish): inject story headlines into News publish copy (Gap #13)

TASK 2 — NBA publish copy quality (Gap #34)  
File: lib/publish.js — find the nba: prompt block
Inject team names and game data from items[].
One commit: fix(publish): inject game data into NBA publish copy (Gap #34)

TASK 3 — Upload-Post per-platform polling (Gap #17)
File: lib/publish.js — after request_id is returned from Upload-Post
Add a fire-and-forget setImmediate poll (every 30s, up to 10 attempts) of the Upload-Post status endpoint. Log per-platform results. Never block the main pipeline — if polling errors, warn and stop.
One commit: feat(publish): poll Upload-Post per-platform status (Gap #17)

RULES:
- Never read lib/publish.js in full — grep -n first, read ±50 lines around the match
- node -c lib/publish.js must pass before every commit
- Update STATUS.md → Last Agent Action table in every commit
- ⛔ NEVER commit to main. Run git branch --show-current before every commit — it must say cline-b/wave0-remaining. If it says main, stop and run git checkout cline-b/wave0-remaining first.
- When done: your final message must include branch name (cline-b/wave0-remaining), commit hash(es), what was done, node -c confirmation

Start: git checkout cline-b/wave0-remaining
```

---

## Cline-C — cline-c/frontend-fixes

```
SHELL RULE: Every grep/find/rg/ls must end with || true — no exceptions. Exit code 1 from a bare search causes an infinite retry loop.

You are Cline-C working on branch cline-c/frontend-fixes.

WHY THIS BRANCH EXISTS:
Two visible frontend problems:
1. Story cards in the News chrome sidebar are barely readable — semi-transparent background bleeds into bright video backgrounds, text is too small, no card border. TV card was removed and story cards are now the primary chrome element but the CSS was never updated to match.
2. Rob consistently sees ≤9 streamers per episode even with 12 listed — because generateTwitch() only looks at the last 24h of clips. Less-active streamers drop silently with no feedback. Also 2 streamers (extraemily, yourragegaming) are missing from the dashboard's DISPLAY_NAMES map.

WHERE TO FIND CONTEXT:
- BRANCH_NOTES.md — exact line numbers, before/after CSS, commit messages
- docs/handoffs/CLINE_HANDOFF_STORY_CARD_VISIBILITY.md — exact CSS changes
- docs/handoffs/CLINE_HANDOFF_TWITCH_12_STREAMER_DROP_VISIBILITY.md — exact JS changes

WHAT TO DO (2 tasks, in order):

TASK 1 — Story card visibility (tools/clipzworld_newscast.html)
4 CSS changes — no logic:
- .story-item background: rgba(13,20,36,0.97) → rgba(8,14,28,1.0), add thin gold border on 3 sides
- .story-item-cat font-size: 11px → 12px
- .story-item-text font-size: 16px → 18px, color: rgba(255,255,255,0.85) → #ffffff
- .story-item.active background: rgba(34,48,75,0.95) → rgba(34,48,75,1.0)
One commit: fix(chrome): story card full white text + opaque bg + subtle border

TASK 2 — Twitch 12-streamer drop visibility (cwn_production.html)
3 changes — no server.js changes:
- Line ~3094: clip window 24h → 7d (86400000 → 86400000*7)
- Lines ~3114-3118: add extraemily and yourragegaming to DISPLAY_NAMES map
- Track droppedStreamers[] across the fetch loop, log to console + show in status bar when streamers are dropped
One commit: fix(dashboard): widen Twitch clip window 24h→7d + surface streamer silent-drops

RULES:
- Never read cwn_production.html or clipzworld_newscast.html in full — grep -n first, read ±50 lines around the match
- Update STATUS.md → Last Agent Action table in every commit
- ⛔ NEVER commit to main. Run git branch --show-current before every commit — it must say cline-c/frontend-fixes. If it says main, stop and run git checkout cline-c/frontend-fixes first.
- When done: your final message must include branch name (cline-c/frontend-fixes), commit hash(es), what was done, node -c confirmation (for any .js files touched)

Start: git checkout -b cline-c/frontend-fixes
```

---

## Cline-C — cline-c/chrome-directives-fix

```
SHELL RULE: Every grep/find/rg/ls must end with || true — no exceptions. Exit code 1 from a bare search causes an infinite retry loop.

You are Cline-C working on branch cline-c/chrome-directives-fix.

WHY THIS BRANCH EXISTS:
The News chrome sidebar shows news outlet names ("AL JAZEERA", "BBC") as the category label on story cards instead of "WORLD NEWS". This happens for directive-sourced stories where directive.flag?.source (the outlet name) is used as the display label. Line 139 is already fixed. Line 148 still has the bug.

WHERE TO FIND CONTEXT:
- BRANCH_NOTES.md — exact line, what to change, verification steps
- docs/handoffs/CLINE_HANDOFF_NEWS_CHROME_FIX.md — full diagnosis

WHAT TO DO (1 task):

TASK — Fix outlet name leak
File: lib/chromeDirectives.js line ~148
Change: category: directive.flag?.source || 'WORLD NEWS'
To:     category: 'WORLD NEWS'
Then scan the rest of the file for any other .source references used as visible category or label fields and fix those too.

One commit: fix(chrome): always show WORLD NEWS category — never outlet name

RULES:
- node -c lib/chromeDirectives.js must pass before committing
- Update STATUS.md → Last Agent Action table in the commit
- ⛔ NEVER commit to main. Run git branch --show-current before every commit — it must say cline-c/chrome-directives-fix. If it says main, stop and run git checkout cline-c/chrome-directives-fix first.
- When done: your final message must include branch name (cline-c/chrome-directives-fix), commit hash(es), what was done, node -c confirmation

Start: git checkout cline-c/chrome-directives-fix
```
