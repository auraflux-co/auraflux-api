# CWN Production — Status & Task Tracker

**Last Updated:** 2026-04-14 (4:10 AM ET)
**Branch:** main | **Latest Commit:** Fix: send scriptForHeygen (plain text) to dashboard, not raw JSON directive
**🚨 NEW ARCHITECTURE DOC:** Every agent must read `GATED_PIPELINE_ARCHITECTURE.md` at the start of every session. It supersedes the ad-hoc retry logic patchwork and defines 9 principles + 7 gates + Gate Output Contract + collaborative QA dialogue pattern.
**How to start a session:** Tell Cline: _"Read CLAUDE.md and STATUS.md and tell me what we're working on"_
**Every morning:** `cat MORNING_BRIEFING.md` — see what Aider did overnight before touching anything

---

## 🗂️ Agent Assignments

| Agent | Role | Best For |
|-------|------|----------|
| **Claude Code** | Creative Director + Architect | HTML template design, brand decisions, spec writing |
| **Cline** | Implementation Lead | server.js edits, API integration, pipeline wiring |
| **Aider** | Surgical Coder | Text generation logic, prompt engineering, keyword detection |

---

## 🤖 Last Agent Action

> **Every agent must update this table before committing code. The pre-commit hook will block commits that skip this.**

| Agent | Task Completed | Files Changed | Commit | Timestamp |
|-------|---------------|---------------|--------|-----------|
| Claude Code | **fix(news/pipeline): wire directive sidecar jobId to assembly, fix isClipMatchOnly, delete dead services/** — Fix 1 of 6 from CLINE_HANDOFF_NEWS_CHROME_FIX.md: cwn_production.html now stores metricsJobId as CURRENT_META.scriptJobId and includes jobId in assembly payload so hasDirectiveForJob() finds the directive sidecar (root cause of missing TV card/flag/sidebar on every News run). isClipMatchOnly logic fixed to use .some(d => d.type !== 'clip_match') so surgical Gate 1 fix path is reachable. services/assembly.js, services/qa.js, services/script_generation.js deleted (dead code, all functions duplicated in server.js). | cwn_production.html, server.js, services/ (deleted) | pending | 2026-04-14 ET |
| Cline | **refactor: code review cleanup — remove unused imports, fix puppeteer crash, hoist cheerio** — Comprehensive code review (19 findings in CODE_REVIEW.md). Safe fixes applied: (1) Removed 4 unused imports: validateChromeScript, withRetry, getFallbackImage, validateUrl. (2) Added execSync to top-level child_process destructure. (3) Fixed C-3 CRITICAL runtime crash: puppeteer.newPage() → browser.newPage() in /news/generate-intro-card route. (4) Improved Gate 6 error logging: now captures e.response?.data (HTTP response body) instead of just e.message. (5) Hoisted cheerio to top-level require (line 100) and removed 3 inline re-requires. Server restarts cleanly. node -c server.js → exit 0. | server.js, CODE_REVIEW.md (new), STATUS.md | pending | 2026-04-14 4:10 AM ET |
| Cline | **fix(news): send scriptForHeygen (plain text) to dashboard, not raw JSON directive** — Root cause of Gate 2 `Expected 2 segments, got 0`: `res.json({ script, ... })` in `/generate-full-script` was returning the raw Gemini JSON directive for News jobs instead of the extracted plain spoken text. Dashboard textarea received JSON → `parseSegments_v2` found 0 `=== HEADER ===` markers → Gate 2 reported `segment_count_mismatch: Expected 2 segments, got 0`. Fix: `res.json({ script: scriptForHeygen, ... })` — `scriptForHeygen` is the plain text extracted by `extractSpokenTextFromDirective()` for News directive mode, or the raw script unchanged for all other content types (Twitch/NBA). Backward-compatible. `node -c server.js` → OK. | `server.js`, `STATUS.md` | pending | 2026-04-14 3:28 AM ET |
| Cline | **Red 4 chrome bug fixes — 4 bugs from asm_1776140626023**: Fix 1 (prev session): Gemini prompt chrome format block rewritten to match ScriptSchema exactly — eliminates schema drift. Fix 2 (prev session): `validateChromeScript()` now called after JSON.parse in assembly path — schema drift is no longer silent. Fix 3a (prev session): `directiveToOverlayParams()` now exposes `tvCard` in return object. Fix 3b (prev session): `generateChromeOverlayFromDirective()` destructures and passes `tvCard` through. Fix 3c (prev session): `generateNewscastOverlay()` accepts `tvCard = null` in options; `page.evaluate()` passes `tvCard` and includes TV card injection code (show/hide `.tv-card`, set `.tv-card-image` src, `.tv-card-headline` text, `.tv-card-source` text). Fix 3d (this session): Added `.tv-card` div to `tools/clipzworld_newscast.html` — CSS positioned at OVERLAY_ZONE (absolute, left:1240px, top:40px, 520×293, navy bg, gold border, hidden by default) + HTML with `.tv-card-image`, `.tv-card-meta`, `.tv-card-headline`, `.tv-card-source` child elements. Fix 4 (prev session): Source clip vfFilter changed from `decrease,pad` (letterbox) to `increase,crop` (zoom-to-fill) — eliminates portrait-with-navy-bars on Al Jazeera clips. `node -c server.js` → exit 0. | `server.js`, `lib/chromeDirectives.js`, `tools/clipzworld_newscast.html`, `STATUS.md` | pending | 2026-04-14 1:25 AM ET |
| Aider | **feat(atlassian): rebuild jira and confluence clients** — Per `OVERNIGHT_TASKS.md` priority, created new client libraries for Jira (`lib/clients/jira_client.js`) and Confluence (`lib/clients/confluence_client.js`) with read-only methods (`healthCheck`, `getProject`/`getSpace`, `listIssues`/`listPages`). Added a CLI ping script (`scripts/jira_ping.js`) to test credentials and connectivity, and a corresponding `npm run jira-ping` script. This unblocks follow-up tasks for Jira-based reporting and work tracking migration. | `lib/clients/jira_client.js` (new), `lib/clients/confluence_client.js` (new), `scripts/jira_ping.js` (new), `.env.example`, `package.json`, `OVERNIGHT_TASKS.md`, `STATUS.md`, `MORNING_BRIEFING.md` | d265017 | 2026-04-14 1:45 AM ET |
| Cline | **feat: restore auto-advance Stage 1→6 (3 fixes)** — Implemented `CLINE_HANDOFF_RESTORE_AUTO_ADVANCE.md`. Fix 1: `pollVideo` onDone callback in `sendToHeyGen()` now checks if all avatar segments are done and auto-fires `triggerGate2(batchJob)` + `assembleJob(batchJob.id)` (3s delay), guarded by `_autoAssembleFired`. Fix 2: Gate 5 PASS block replaced — instead of showing APPROVE & UPLOAD button, auto-fires `autoGeneratePublishPrep(job.id)` with 1500ms delay, shows status banner + REJECT escape hatch, guarded by `_autoPublishFired`. Fix 3: new `autoGeneratePublishPrep(jobId)` headless function inserted before Gate 3 comment — calls `/generate-publish-copy`, stores result on `job.publishPrep`, then calls `approveAndUpload(jobId)`; on failure shows manual fallback message. JS syntax verified (`node --check` exit 0). Per `GATED_PIPELINE_ARCHITECTURE.md`: only Gate 7 (Rob reviews private drafts on platform) is a human checkpoint. | `cwn_production.html`, `STATUS.md` | pending | 2026-04-14 12:20 AM ET |
| Cline | **Option Y hotfix 1 — feat(news): browser-like headers on all Al Jazeera axios calls to bypass WAF**: Al Jazeera WAF blocks axios default UA (`axios/1.x.x`) but allows full Chrome 132 on macOS header set. Added `BROWSER_HEADERS` constant near top of `server.js`. Applied to all 3 Al Jazeera axios call sites: (1) `GET /news/us-canada-videos` HTML scrape; (2) `scrapeArticleVideo()` article fetch; (3) `scrapeArticleOgImage()` og:image fetch. `node -c server.js` exit 0. Smoke test: `{ok:true, totalFound:6, validationSummary:{passed:6, warnings:0, failed:0}}`. | `server.js`, `STATUS.md` | pending | 2026-04-13 11:28 PM ET |
| Claude Code | **Red 4 hotfix 9: Gate 2 validator News math (5 scenes per story) + derive N from items** — News smoke test #11 v8 displayed two stacked errors: (1) Fix 25c's NEWS_CLIP_GATE_FAIL 403 from Al Jazeera (handed off to Cline as Option Y in `CLINE_HANDOFF_AL_JAZEERA_403_WORKAROUND.md`) and (2) Gate 2 SEGMENT STRUCTURE FAIL with `Expected 2 segments, got 27` + `Config: 0 streamers × 2 clips = 2 expected`. Root cause of #2: `gate2_validateSegmentStructure()` at `cwn_production.html` used the old 3-scene-per-story math for News (`1 + N * 3 + 1`) but hotfix 6 switched News to 5 scenes per story (`1 + N * 5 + 1 = 27 for 5 stories`). Also `N = (config.streamers || []).length` which is 0 for News because News stores stories in `orderedClipUrls`, not `streamers`. Two-part fix: (a) split News from NBA in the validator's contentType switch — News uses `1 + N * 5 + 1`, NBA keeps `1 + N * 3 + 1` NARRATION math; (b) derive N for News from `config.items.length` OR `window.CURRENT_META.orderedClipUrls.length` OR `config.streamers.length` (fallback chain); (c) update `sendToHeyGen()` `gate2Config` to pass `items: CURRENT_META.orderedClipUrls`. Hotfix 10 (parseSegments race condition) was considered unnecessary — hotfix 8's 500ms setTimeout already guarantees sendToHeyGen runs after orderedClipUrls is stored, so parseSegments inside sendToHeyGen reads the populated array. Option Y (Al Jazeera 403) is Cline's lane — this hotfix covers Option X only. Both Option X + Option Y need to land before test #11 v9 can run end-to-end. | `cwn_production.html` (gate2_validateSegmentStructure + sendToHeyGen gate2Config), `STATUS.md` | pending | 2026-04-13 11:45 PM ET |
| Claude Code | **Red 4 hotfix 8: restore auto-advance from Gate 1 PASS to sendToHeyGen()** — News smoke test #11 v7 Gate 1 PASSED at 22:34 ET for the second consecutive time (first clean Red 4 directive path pass was 21:44 ET v6). Both runs silently died after Gate 1 with no HeyGen submissions fired, no tmp files, no job card persisted. Rob flagged: *"shouldnt need to click the button though did something stop the auto process of when script passes it goes right to heygen?"* Root cause found via code audit of `displayScriptQA()` at `cwn_production.html:5360`: when `scriptQA.outcome === 'pass'` the UI pushes a `<div>` saying *"✅ Auto-proceeding to HeyGen — no issues found"* but **no code actually calls `sendToHeyGen()`**. The UI text is a lie. All 5 callers of `callFullScriptServer` pass `null` as the `onDone` callback. The auto-advance behavior was lost somewhere in the Red 4 / Track C / hotfix series tonight. Console log `[gate1] ✅ Script QA auto-passed (100/100) — HeyGen send unlocked` says "unlocked" not "fired" — "unlocked" only means the manual SEND TO HEYGEN button at line 285 becomes clickable. Rob was right — the legacy flow auto-fired; tonight's dashboard doesn't. Hotfix 8: add `setTimeout(() => sendToHeyGen(), 500)` inside the `outcome === 'pass'` branch of `displayScriptQA()`. 500ms delay lets `callFullScriptServer`'s xhr.onload finish storing `orderedClipUrls` into `window.CURRENT_META` before `sendToHeyGen()` calls `parseSegments()` — avoids the race condition from the 22:34 run where parseSegments ran with empty clipUrls. Manual review path unchanged (still requires button click). Hard fail path unchanged (no button). | `cwn_production.html:5376-5391` (12 lines added), `STATUS.md` | pending | 2026-04-13 11:15 PM ET |
| Claude Code | **Red 4 hotfix 7: dashboard parseSegments_v2 detects JSON directive scripts + JSON-aware segment builder** — News smoke test #11 v6 Gate 1 PASSED 100/100 at 21:44 ET (first ever Red 4 directive path pass — Claude verified 27 scenes, 5 source_clip scenes, empty spokenText on clips, source attribution clean). BUT pipeline died immediately after Gate 1 with no HeyGen submissions, no tmp files, no job card persisted. Root cause: `parseSegments_v2()` at `cwn_production.html:3425` splits scripts by `=== HEADER ===` text markers and detects clips via `[CLIP PLAYS HERE]` regex — BOTH removed by hotfix 6's prompt rewrite. JSON scripts have no text markers → rawSections = [] → zero segments built → HeyGen never called → job card never saved → silent failure. The dashboard was still expecting the OLD text format even though the server now produces JSON. Server-side Gate 1 fixes (hotfixes 4-6) didn't cover this client-side consumer. Hotfix 7 adds: (1) `dashStripCodeFences()` helper mirroring `server.js:14`; (2) `parseSegments_v2_json()` new function that iterates `parsed.scenes[]`, emits avatar segments with `scene.spokenText` as HeyGen input + chrome directive attached, emits source_clip segments with URLs resolved from `orderedClipUrls[clipInsertIdx]` by index; (3) JSON detection probe in `parseSegments_v2` — if script starts with `{` after fence stripping and parses with valid `scenes[]` array, route to JSON parser; else fall through to legacy text parser unchanged (Twitch/NBA paths unaffected). 85 lines of new dashboard JavaScript, zero server.js changes. | `cwn_production.html` (85 lines added in parseSegments area), `STATUS.md` | pending | 2026-04-13 10:30 PM ET |
| Claude Code | **Red 4 hotfix 6: unambiguous 5-scene-per-story News structure with standalone source_clip scenes** — News smoke test #11 v5 hit new Gate 1 hard-fail (27 scenes vs 22 expected, 0 clip markers vs 5 expected). Root cause: Cline's Red 4 News Gemini prompt was HYBRID — instructed Gemini to both (a) write `=== HEADER ===` text-format with `[CLIP PLAYS HERE]` text markers inside SETUP scenes (4 scenes per story), AND (b) wrap the output in JSON with `type: 'avatar'|'source_clip'` scenes. Gemini alternated between strategies across runs (v4 → text markers, v5 → standalone source_clip scenes). Rob's call 2026-04-13 9:15 PM: "i want all the features in because i can post without them" → ship forward, make prompt unambiguous, enforce ONE structure. Picked Option A2 (standalone source_clip scenes, 5 per story × N + 2 fixed = 27 for 5-story episode) because it matches `CHROME_DIRECTIVE_ARCHITECTURE.md` spec and cleans up the hybrid mess. Six edits in one commit: (1) sceneHeaders generator adds `STORY#_CLIP` between SETUP and SUMMARY; (2) prompt story-count line says "source_clip scenes required" instead of "`[CLIP PLAYS HERE]` markers required"; (3) SCENE LENGTH RULES + CONTENT STRUCTURE PER SCENE blocks rewritten to document clip scenes as `type: source_clip` with empty spokenText, no more `[beat]` + `[CLIP PLAYS HERE]` + `[beat]` text pattern in SETUP scenes, added OUTRO "Appreciate you" requirement; (4) VALIDATION CHECKLIST now counts source_clip scenes instead of text markers; (5) `expectedScenes` formula `items.length * 4` → `items.length * 5`; (6) Claude News Gate 1 checklist updated — 5 separate scenes instead of 4, checks JSON scenes[] array instead of `=== HEADER ===` markers, checks for source_clip scenes with empty spokenText instead of `[CLIP PLAYS HERE]` markers, checks OUTRO spokenText for "Appreciate you", checks [beat] rules removed (no beats in pure directive mode). Also reverted hotfix 5's spokenText marker scan back to `filter(s.type === 'source_clip').length` since the prompt no longer puts markers in spokenText. | `server.js` (6 edits — prompt rewrite + expectedScenes formula + checklist update + hotfix 5 revert), `STATUS.md` | pending | 2026-04-13 9:15 PM ET |
| Claude Code | **Red 4 hotfix 5: Gate 1 clip count scans spokenText instead of scene.type** — Hotfix 4 fixed scene count but test #11 v4 hit new hard-fail on clip count: "Found 0 [CLIP PLAYS HERE] markers, expected 5" even though Claude's content review saw all 5 clips. Root cause (investigated by reading Cline's News Gemini prompt at `server.js:7589-7717`): Cline's prompt is **HYBRID** — instructs Gemini to write scenes with `=== HEADER ===` + `[beat] [CLIP PLAYS HERE] [beat]` text markers, THEN wrap the whole thing in JSON with `scenes[]` where each scene has `type: 'avatar'|'source_clip'` and `spokenText`. Gemini follows BOTH instructions: 22 scene objects all with `type: 'avatar'`, clip markers embedded as literal text inside STORY#_SETUP spokenText fields. **NO scene has `type: 'source_clip'`.** My hotfix 4 counted `scenes.filter(s.type==='source_clip').length` = 0 even though Claude's text review found 5 `[CLIP PLAYS HERE]` markers in the concatenated spokenText. Fix: scan concatenated spokenText for the marker regex, same pattern as legacy mode, just applied to the spokenText aggregation instead of raw script. This works around Cline's prompt hybrid — future directive work should eventually decide whether to (a) have Gemini emit standalone source_clip scenes OR (b) keep the marker-in-text pattern. For now marker scanning works. Hotfix 4's scene count, appreciate-you, and JSON validation blocks are all unchanged. | `server.js:2445-2460`, `STATUS.md` | pending | 2026-04-13 9:00 PM ET |
| Claude Code | **Red 4 hotfix 4: Gate 1 directive-mode-aware scene/clip/outro counting** — News smoke test #11 v3 hit new Gate 1 hard-fail even though Claude's raw review gave 100/100 and source attribution was clean (hotfix 1 worked). Root cause: legacy text regexes at `server.js:2425/2428/2431` scan for `===\s+[A-Z_0-9]+\s+===` markers and literal `[CLIP PLAYS HERE]` / `appreciate you` text. JSON directive scripts don't contain that syntax — Gemini's output has `{"id": "STORY1_INTRO", "spokenText": "..."}` instead of `=== STORY1_INTRO ===\n...`. Regex found 0 scene markers → `wrongSceneCount = true` → -25 "SCENE COUNT: Found 0 scenes, expected 22" deduction fired → hard fail despite content being perfect. Fix: parse the JSON up-front (with `stripCodeFences`) when `contentType==='news' && USE_DIRECTIVE_CHROME`, compute `sceneMarkers = parsedDirectiveJson.scenes.length`, `clipMarkers = filter(s.type==='source_clip').length`, `missingAppreciateYou` by scanning concatenated `scenes[].spokenText`. Legacy text regex still runs in fallback when JSON parse fails (so the -20 NEWS JSON DIRECTIVE deduction still catches real failures). Cline's Red 4 implementation left legacy text scans in place alongside new JSON scene counting — both ran in parallel, legacy won the "wrongSceneCount" decision and hard-failed Gate 1. This is the 4th Red 4 followup hotfix but they're ALL the same class: new code added alongside legacy code that now sees data shapes it was never designed for. | `server.js:2424-2470`, `STATUS.md` | pending | 2026-04-13 8:50 PM ET |
| Claude Code | **Red 4 hotfix 2+3: News endpoint 24h→48h lookback + end-of-day timestamp parse + Track C validateVideo uses scrapeArticleVideo instead of direct yt-dlp** — Two cascading bugs surfaced within 20 min of shipping Red 4 and Track C. Bug 2: News smoke test #11 UI returned `items must be array with at least 1 item(s)` because `/news/us-canada-videos` endpoint returned `recentCount: 0` — root cause: AJ URL dates parsed as start-of-day (`T00:00:00Z`), 24h lookback cutoff at 00:30 UTC on 4/14 made every 4/13 video 24.5h old and filtered out. Fix: extend lookback to 48h (`server.js:5685`) AND parse URL dates as end-of-day `T23:59:59Z` (`server.js:5794`) — belt-and-suspenders. Bug 3: after fix 2 landed, all 5 videos returned but `validation.status = fail` because Track C's `validateVideo()` at `server.js:5711` called `yt-dlp` directly on AJ article URLs which return `Unsupported URL`. Root cause: Track C skipped Fix 9's JSON-LD `scrapeArticleVideo()` intermediate step (Fix 9 fetches article HTML → extracts Brightcove `VideoObject.embedUrl` → runs yt-dlp on the embed URL, which DOES work). Fix: rewrite `validateVideo()` Check 2 to call `scrapeArticleVideo(v.url)` — returns non-null HLS URL on success, null on failure. Dimensions/duration checks deferred to Track C v2 (would need separate ffprobe on HLS URL). Live endpoint verification: `{passed: 5, warnings: 0, failed: 0}` — all 5 Al Jazeera videos now pass validation. Dashboard will show 5 green-dot story cards all selectable. | `server.js` (3 edits — 5685 lookback, 5794 end-of-day parse, 5705-5748 validateVideo rewrite), `STATUS.md` | pending | 2026-04-13 8:40 PM ET |
| Claude Code | **Red 4 hotfix: stripCodeFences() for Gemini JSON markdown fence wrapping + source attribution rule reinforcement** — News smoke test #11 hit 3 consecutive Gate 1 hard-fails (logs 20:00/20:01/20:02 ET) because Gemini 2.5 Flash wrapped directive JSON output in ```json ... ``` markdown fences despite prompt. `JSON.parse` choked on backticks, Gate 1 fired -20 "not valid JSON" + -25 scene count (downstream empty parsed array) = 30/100 hard fail. **Zero HeyGen spend** — Red 4 Gate 1 validation worked as designed, caught at cheapest stage. Hotfix: (1) `stripCodeFences()` helper at `server.js:14` handles ```json/```JSON/plain ``` patterns, safe no-op on raw JSON; (2) Gate 1 parse at `server.js:2618` calls it before `JSON.parse`; (3) assembly directive parse at `server.js:4131` calls it before `JSON.parse`; (4) source attribution rule at `server.js:7603` strengthened from 4 to 8 banned patterns + explicit "drop attribution, state fact as Bobby G's own observation" guidance + retry-budget warning. Second fix addresses separate Gemini drift Gate 1 also caught on test #11 ("According to a direct statement from the E-U" in STORY5_SETUP). `node -c server.js` exit 0. | `server.js` (4 edits), `STATUS.md` | pending | 2026-04-13 8:15 PM ET |
| Cline | **Red 2 — feat(news): mask Al Jazeera corner watermark with CWN navy box**: Appended `,drawbox=x=1780:y=960:w=120:h=80:color=0x0d1424@1.0:t=fill` to the non-avatar vfFilter chain in the News source_clip normalization pass (`server.js` ~line 4376). Condition: `contentType === 'news' && !isAvatarSeg`. Covers Al Jazeera bottom-right corner watermark (logo + 20px safety padding) with CWN navy `#0d1424` — matches letterbox bar color so it reads as intentional CWN framing. `node -c server.js` → exit 0. | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | pending | 2026-04-13 7:10 PM ET |
| Cline | **Red 3 — feat(news): skip Al Jazeera intro branding cards with -ss 5 fast-seek**: Added `NEWS_CLIP_INTRO_SKIP = 5` constant and prepended `-ss 5` BEFORE `-i` in FFmpeg args for News source_clip segments in `buildTsArgs()` (`server.js` ~line 4401). Fast-seek mode (keyframe-accurate, no decode overhead). Effective clip window: 5s-30s of source (25s cap still applies after offset). Condition: `contentType === 'news' && !isAvatarSeg`. `node -c server.js` → exit 0. | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | pending | 2026-04-13 7:20 PM ET |
| Cline | **Red 4 — feat(news): proactive chrome directive architecture (Red 4 / Track B)**: Full 7-subtask rewrite. (4.1) `lib/chromeDirectives.js` — new file with Zod schema (`ChromeDirectiveSchema`, `SceneSchema`, `ScriptSchema`), `validateScript()`, `directiveToOverlayParams()`, `extractSpokenText()`. (4.2) News Gemini prompt rewritten to output entire script as a single JSON object with `storyList`, `brandConfig`, and `scenes[]` arrays — each scene has `id`, `type`, `spokenText`, `chrome` fields. HeyGen still receives plain text extracted via `extractSpokenText()`. (4.3) `claudeScriptQA()` Gate 1 QA extended with JSON schema validation for News scripts — parses JSON, checks `scenes[]` array exists and each scene has `id`/`type`/`chrome` fields, deducts 10-20 pts on failure. (4.4) Assembly chrome burn rewritten as `burnSceneChromeFromDirective()` consumer. (4.5) `generateChromeOverlayFromDirective()` helper added. (4.6) `USE_DIRECTIVE_CHROME` feature flag (`process.env.USE_DIRECTIVE_CHROME !== 'false'`, default true) guards new path; legacy Fix 5/7 state machine preserved as fallback. (4.7) dead-code check excluded per handoff spec. `node -c server.js` → exit 0. | `server.js`, `lib/chromeDirectives.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | pending | 2026-04-13 7:30 PM ET |
| Claude Code | **CLINE_HANDOFF_NEWS_FULL_FIX_BEFORE_TEST_10.md** — Rob directive 2026-04-13 PM: *"i dont want to keep testing if its not going to pass the requirements of the set design... we wouldnt produce a show live if had all of these issues, so i want to get everything in and then fix from there"* + *"cline has never taken longer than 20mins just fyi -- just ship everything now that is for news getting past"*. 4-commit handoff covering ALL 4 red items before test #10 fires: (1) Red 2 watermark mask via FFmpeg `drawbox` in News source_clip filter chain — 120×80 region at (1780, 960) filled with `#0d1424` CWN navy; (2) Red 3 intro card skip via `-ss 5` BEFORE `-i` for fast-seek mode, effective clip window becomes 5s-30s of source; (3) Red 4 FULL proactive chrome directive architecture rewrite — new `lib/chromeDirectives.js` with Zod schema + `validateScript()`, News Gemini prompt outputs JSON with per-scene chrome directives, Claude Gate 1 QA parses JSON + validates schema + content checks, assembly chrome burn rewritten as `burnSceneChromeFromDirective()` consumer, new `generateChromeOverlayFromDirective()` helper, feature flag `USE_DIRECTIVE_CHROME` for emergency rollback with legacy Fix 5/7 state machine dead-coded; (4) Red 1 residual surgical bandaids applied ONLY after test #10 review reveals specific issues. Recalibrated Track B from 9-13 days (human velocity) to ~3 hours (Cline 20-min-per-task velocity). Total estimated Cline time: ~3.5-4 hours. Test #10 fires ONLY after all 4 commits land. | `CLINE_HANDOFF_NEWS_FULL_FIX_BEFORE_TEST_10.md` (new), `STATUS.md` | pending | 2026-04-13 6:40 PM ET |
| Cline | **Track C — per-video validation pass + dashboard badges [smoke-test-10]**: Extended GET /news/us-canada-videos with validateVideo(v) async function — 5 checks in parallel per video: (1) Brightcove URL HEAD 3s timeout, (2) yt-dlp --skip-download --dump-json 10s timeout, (3) dimensions >= 1280x720, (4) duration > 5s (warning if > 120s), (5) og:image HEAD 3s timeout. Each video gets validation: { status, checks, issues[] } where status is ok/warning/fail. Response includes validationSummary: { passed, warnings, failed }. Validation skippable via ?validate=false. Dashboard: fetchCwnNewsVideos() now preserves validation per item + validationSummary on response object. thumbLoadNewsStories() passes both to thumbRenderNewsStoryGrid(). Grid renderer: summary bar above grid (green/amber/red dot counts), per-card badge dot (green=ok, amber=warning, red=fail), fail cards dimmed to 60% opacity + pointer-events:none + "UNUSABLE" label instead of "USE THIS STORY" button. node -c server.js exit 0. | server.js, cwn_production.html, STATUS.md, LONGFORM_FIX_ROTATION.md | pending | 2026-04-13 6:20 PM ET |
| Claude Code | **News smoke test #10 handoff + Chrome Directive Architecture doc** (3 tracks from Rob's 2026-04-13 PM review session). Track A: `CLINE_HANDOFF_NEWS_SMOKE_TEST_10_FIXES.md` — Tier 1 emergency fix for 2 visual catastrophes from test #9: (1) clips cropped chin-to-tie because `server.js:4377` used `force_original_aspect_ratio=increase,crop=1920:1080` zoom-to-fill on non-16:9 Al Jazeera sources, fix: swap to `decrease,pad` with `color=0x0d1424` (CWN dark navy letterbox); (2) clips running 50-123 seconds (full Al Jazeera news packages, not highlights), fix: hard cap at `NEWS_CLIP_MAX_SECONDS=25` applied after Fix 9's silencedetect via `Math.min(silencedetect, cap)` logic. Track C: same handoff — extends `/news/us-canada-videos` endpoint with 5-check parallel validation pass (Brightcove reachable, yt-dlp extract, dimensions, duration, og:image reachable) + dashboard badges per story card (green/amber/red) + validationSummary aggregate. Catches problems upstream before HeyGen spend (~$0.86 saved per prevented failed run). Track B: `CHROME_DIRECTIVE_ARCHITECTURE.md` — parked design doc capturing Rob's proactive vs reactive architectural direction. Gemini writes chrome directives into the script at generation time, assembly reads them literally — no state machine, no string matching, no inference. Full schema, migration path (5 phases, 9-13 days effort), risks, alternatives considered. NOT a handoff — reference material for when Rob says "it's time" (likely Phase 1→2 transition). | `CLINE_HANDOFF_NEWS_SMOKE_TEST_10_FIXES.md` (new), `CHROME_DIRECTIVE_ARCHITECTURE.md` (new), `STATUS.md` | pending | 2026-04-13 6:00 PM ET |
| Claude Code | **Fix 25b regression** — `fetchCwnNewsVideos()` at `cwn_production.html:2989` missed the `CFG.ffmpegUrl` prefix. When dashboard is served from `http://localhost:8765` (Python static), relative `/news/us-canada-videos` resolved to the Python server → 404 HTML → `JSON.parse` threw `Unexpected token '<', "<!DOCTYPE "... is not valid JSON` → News compilation card showed error pill and blocked smoke test #9 from firing. Every other xhr.open in the file uses `CFG.ffmpegUrl + '/endpoint'` pattern (30+ refs). 1-line fix. Verified backend at port 3000 returns `HTTP 200` + `application/json` + 5 videos found + 4 in 24h window. Rob hard-refreshes dashboard and re-fires News smoke test #9. | `cwn_production.html`, `STATUS.md` | pending | 2026-04-13 5:00 PM ET |
| Cline | **Smoke test #9 pre-flight — Fix 25a/25b/25c + Fix 28 + Fix 27b**: Root cause of News 0-clip runs: dashboard fetched from global Al Jazeera RSS (all.xml via rss2json, ~20-30% video hit rate) instead of aljazeera.com/us-canada/ HTML page /video/newsfeed/ paths (100% video by construction). Fix 25a (prev session): new GET /news/us-canada-videos endpoint in server.js — HTML scrapes US/Canada section, returns only /video/newsfeed/ articles. Fix 25b: cwn_production.html — all 5 rss2json references replaced with fetchCwnNewsVideos() adapter function; thumbLoadNewsStories() refactored + thumbRenderNewsStoryGrid() extracted. Fix 25c (prev session): pre-Gate-0 hard gate (NEWS_CLIP_GATE_FAIL) in server.js News block — blocks episode production before any Gemini/Claude/HeyGen spend if any selected story lacks video. Fix 28: filename generator now uses actualClipCount = segsToProcess.filter(s => s.type === 'source_clip').length instead of count embedded in jobTitle string. Fix 27b: added "AI Video Analysis — Known Reliability Limits" section to GATED_PIPELINE_ARCHITECTURE.md — documents Gemini 2.5 Flash hallucination finding (fabricates clip presence/timestamps when prompted with expected count; smoke test #8 evidence: 4 of 5 clips fabricated), rule: never prompt Gemini with expected clip counts. grep -n "rss2json" cwn_production.html returns only comments — zero live URL references. node -c server.js exit 0. | cwn_production.html, server.js, GATED_PIPELINE_ARCHITECTURE.md, STATUS.md, LONGFORM_FIX_ROTATION.md | pending | 2026-04-13 4:46 PM ET |
| Claude Code | **RETRACT & REFRAME** — `scripts/audit_news_clips.js` deleted + News smoke test #9 handoff drafted with root-cause fix. The audit script's first-run report claiming "all 5 clips present" was proven wrong via ffmpeg frame-extraction: only 1 of 5 clips exists (story 5 Lafarge at 03:45), the other 4 frames all show Bobby G. Gemini 2.5 Flash hallucinated 4 clips by inferring plausible content from Bobby G's narration audio when prompted with "there should be 5 clips." Temperature 0.1 did not prevent the hallucination. Root cause found: dashboard's News fetch hits `aljazeera.com/xml/rss/all.xml` via rss2json (global text-heavy RSS) instead of `aljazeera.com/us-canada/` HTML page's `/video/newsfeed/` paths (100% video by construction). Wrote `CLINE_HANDOFF_NEWS_SMOKE_TEST_9_FIXES.md` specifying 5 fixes: Fix 25a new `GET /news/us-canada-videos` endpoint (HTML scrape with env-var override for future RSS.app swap), Fix 25b dashboard source switch, Fix 25c pre-Gate-0 hard gate (no clips no production), Fix 28 filename metadata count real clip files, Fix 27 delete audit script + document Gemini hallucination rule in GATED_PIPELINE_ARCHITECTURE.md. Also specced Fix 30 as follow-up: RSS.app custom feed migration once Rob sets up the account. No HeyGen spend on test #9 until all 5 fixes ship. | `scripts/audit_news_clips.js` (deleted), `CLINE_HANDOFF_NEWS_SMOKE_TEST_9_FIXES.md` (new), `STATUS.md` | pending | 2026-04-13 PM |
| Cline | **Fix 5 (smoke test #8) — fix(news): sidebar 5-cap + mutual exclusion + flag persistence**: Fix 5a: CSS `nth-child(n+6)` caps sidebar at 5 story cards. Fix 5b: `body.sidebar-hidden` mutual exclusion hides sidebar when flag+TV card are active. Fix 5c: state machine split into `isStoryIntro`/`isStoryBody`/default branches — INTRO: `hideSidebar:true` on both overlay PNGs; SETUP/SUMMARY/REACTION: `showLowerThird:true, hideSidebar:false` (flag persists); COLD_OPEN/OUTRO: `showLowerThird:false, hideSidebar:false`. | `server.js`, `tools/clipzworld_newscast.html`, `STATUS.md` | pending | 2026-04-13 11:06 AM ET |
| Cline | Fix 6: orderedClipUrls null-preserve alignment — heygen-poller null-skip: `clipIdx++` always increments (index alignment), then only pushes to `segmentData` if `clip && clip.url`. Null entries (stories without clips) are skipped without crashing. | server.js, STATUS.md | pending | 2026-04-13 AM ET |
| Cline | Fix 1+9: Force 16:9 aspect ratio on News source clips (scale+crop FFmpeg pass) + strip Al Jazeera red outro branding (silencedetect trim). Two helpers: `detectTrailingSilence()` + `computeNewsClipTrimDuration()`. Async `buildTsArgs()` IIFE in normalization pass — for News non-avatar segs, computes trim duration and adds `-t trimDuration` to FFmpeg args. | server.js, STATUS.md, LONGFORM_FIX_ROTATION.md | pending | 2026-04-13 AM ET |
| Cline | Fix 3: Remove spoken source attribution from News Gemini prompt + claudeScriptQA Gate 1 QA (Bobby G must never speak source names) | server.js, STATUS.md, LONGFORM_FIX_ROTATION.md | pending | 2026-04-13 AM ET |
| Claude Code | Queue `scripts/jira_morning_report.js` as 2nd Aider overnight task (dependent on the Atlassian rebuild task shipping first): pulls CPD board state via the Jira client layer, formats to `MORNING_JIRA_REPORT.md` at repo root, hooked into `overnight_runner.sh` so it regenerates nightly next to `MORNING_BRIEFING.md`. Delivers the "morning Jira summary" Rob wanted via Rovo, but without waiting on Rovo — Rovo has no public REST API yet (Forge-only agents, no scheduled summaries), rollout is also org-waitlisted. Swap-to-Rovo path preserved: when Rovo API ships, replace the script body with an agent call, interface stays identical (markdown file in repo root). | `OVERNIGHT_TASKS.md`, `STATUS.md` | pending | 2026-04-13 AM |
| Claude Code | Reset broken Atlassian scaffolding from commit `7eb780f`: the 3 scripts (`atlassian_setup.js`, `jira_sync.js`, `confluence_sync.js`) imported `lib/clients/jira_client` + `lib/clients/confluence_client` which were never written, so all 3 crash on load with MODULE_NOT_FOUND. Deleted all 4 files (3 scripts + `docs/JIRA_CONFLUENCE_MIGRATION_PLAN.md` — generic plan that didn't match Rob's revised architecture). Stripped the 3 phantom npm script entries from `package.json`. Queued replacement task in `OVERNIGHT_TASKS.md` INDEPENDENT section with correct project/space keys (`CPD`/`CP`, not `CWN`/`CWN`) and read-only-client-layer-first scope (healthCheck + getProject + getSpace, no sync logic until pipe is proven open via `scripts/jira_ping.js`). | `scripts/atlassian_setup.js` (deleted), `scripts/jira_sync.js` (deleted), `scripts/confluence_sync.js` (deleted), `docs/JIRA_CONFLUENCE_MIGRATION_PLAN.md` (deleted), `package.json`, `OVERNIGHT_TASKS.md`, `STATUS.md` | pending | 2026-04-13 AM |
| Aider | Add `express-validator` checks to POST endpoints for security. | `server.js`, `OVERNIGHT_TASKS.md`, `STATUS.md`, `MORNING_BRIEFING.md` | pending | 2026-04-13 1:30 AM ET |
| Aider | Fix News/NBA thumbnail 500 errors by replacing Puppeteer with node-canvas. | `server.js`, `OVERNIGHT_TASKS.md`, `STATUS.md`, `MORNING_BRIEFING.md` | pending | 2026-04-12 1:30 AM ET |
| Cline | Added pre-commit hook + STATUS enforcement | `.git/hooks/pre-commit`, `STATUS.md`, `COMMIT_CHECKLIST.md`, `CLAUDE.md` | `a7b6698` | 2026-04-09 7:00 PM ET |
| Claude Code | Newscast UI updates + NBA intro card handoff + Upload API spec | `tools/clipzworld_newscast.html`, `POST_PUBLISH_MANUAL_CHECKLIST.md`, `CLINE_HANDOFF_NBA_INTRO_CARD.md`, `UPLOAD_API_SPEC.md`, `data/upload_status.json` | `b3fcdff` | 2026-04-09 (earlier today) |
| Cline | Phase 1-6 acceptance test plan + rollback process | `test/PHASE_1_6_ACCEPTANCE_TEST.md`, `STATUS.md` | `787f81f` | 2026-04-09 7:26 PM ET |
| Cline | Fix TV card overlay position: OVERLAY_ZONE + 2 hardcoded FFmpeg overlays → x=40 (top-left, facing Bobby G) | `server.js` | `787f81f` | 2026-04-09 7:37 PM ET |
| Cline | Strict clip quota + client-side dedup (7d TTL) + dropdown persistence | `cwn_production.html` | `cd0116e` | 2026-04-11 9:22 PM ET |
| Claude Code | Fix Gate 1 clipsPerStreamer source-of-truth bug: derive from `items[0].clips.length` instead of `req.body.clipsPerStreamer \|\| 2` — dashboard `callFullScriptServer` never sent it, so server was grading Gemini's (correct) 1-clip script against hardcoded 2. Three Gate 1 call sites updated (server.js:6978 new constant, :6983 initial QA, :7016 claudeScriptFix, :7026 post-fix re-QA). Breaks the Gate 1 retry loop Rob hit on the Jason × 1-clip test. | `server.js`, `STATUS.md` | `f8395b7` | 2026-04-11 10:15 PM ET |
| Claude Code | Ticker revert 72→64 (fix gap), intro card 3.5→10s (config-driven, all 3 content types), long-form clip window 7d→48h, POST_PUBLISH_TASKS.md created with full findings from Twitch long-form review | `lib/config.js`, `server.js`, `cwn_production.html`, `POST_PUBLISH_TASKS.md`, `STATUS.md` | `7a957dc` | 2026-04-12 12:30 AM ET |
| Claude Code | Fix Jay Cinco intro card missing: underscore→space normalization in streamer lookup (server.js:3714). Scene headers use underscores, displayNames use spaces — lookup failed silently for multi-word names. | `server.js`, `STATUS.md` | `845a652` | 2026-04-12 1:00 AM ET |
| Claude Code | Sync all ticker speeds to 50px/sec (News+NBA were 80, Twitch was 50). Fix missed News introDur hardcode 3.5→CONFIG. Ticker cache cleared. | `server.js`, `tools/cwn_combined_ticker.html`, `tools/sports_ticker.html`, `STATUS.md` | pending | 2026-04-12 1:30 AM ET |
| Cline | **Fix 1/4 — feat(news): build orderedClipUrls for News in generate-full-script**: Root cause of 22_avatar_0_clips — `orderedClipUrls` was only populated in the Twitch block. News block now maps `items[]` → `orderedClipUrls[]` with url/clipUrl/pageUrl/label/streamer/title fields, filtered to items with valid clip URLs. Fixes the 0-clip assembly for all News long-form jobs. | `server.js`, `STATUS.md` | `e17e647` | 2026-04-12 5:00 PM ET |
| Cline | **Fix 2/4 — feat(news): attach cardData to STORY#_INTRO segments + persist newsItems on job card**: (A) heygen-poller loop: TDZ fix (`contentType` → `card.contentType \|\| 'twitch'`) + attach `cardData` (title/category/storyId/imageUrl/source) to each STORY#_INTRO avatar segment so News TV card burn has story metadata. (B) Job card save block: persist `newsItems[]` (title/source/category/thumbnailUrl/videoUrl/link) so heygen-poller can look up story data after server restart. (C) `allNewsIntros` filter: regex updated from `\(INTRO\)` → `STORY\d+_INTRO \|\| \(INTRO\)` to match actual scene label format. Fixes blank/missing News TV card overlays. | `server.js`, `STATUS.md` | `eb67b0e` | 2026-04-12 5:10 PM ET |
| Cline | **Fix 3/4 — feat(gate3): Gate 3 blind-spot detection for missing clips + TV card**: `geminiQACheck()` now detects when source clips were expected but absent from the assembled video. Added `clipsExpectedButMissing` flag (fires when `clipCount > 0` and Gemini reports no source clips detected). Wired into `hasCriticalFail` (auto-fail), deductions (-25 pts), EARLY checklist (TV CARD), MIDDLE checklist (SOURCE CLIPS), and why-doc CRITICAL FAILURES section (`Clips missing: 🚨 YES/✅ No`). Prevents Gate 3 from scoring 93/100 on a News video with 0 clips. | `server.js`, `STATUS.md` | `9271297` | 2026-04-12 5:20 PM ET |
| Cline | **Fix 4/4 — fix(assembly): News 22-segment all-avatar jobs use concat demuxer not xfade**: Added `(contentType === 'news' && tsFiles.length > 10)` to the concat demuxer condition in long-form assembly. News all-avatar jobs (clipCount=0, tsFiles=22) were falling through to the broken xfade branch (threshold was `>30 segments OR clipCount>0`). xfade offset math is broken when mixing avatar crossfades (0.3s) with clip hard cuts (0.001s) — `cumulativeDur` accumulates wrong offsets causing freeze. Concat demuxer gives hard cuts everywhere but is rock-solid reliable. xfade path still used for avatar-only Twitch/NBA jobs under 30 segments. | `server.js`, `STATUS.md` | `c4c7954` | 2026-04-12 5:30 PM ET |
| Cline | Module split Phase 1: extract lib/config.js, lib/logger.js, lib/metrics.js from server.js; remove inline duplicates; server verified clean | `lib/config.js`, `lib/logger.js`, `lib/metrics.js`, `server.js`, `SERVER_SPLIT_PLAN.md` | `787f81f` | 2026-04-09 11:30 PM ET |
| Cline | Upgrade pre-commit hook: add soft-warn for stale .md docs; update COMMIT_CHECKLIST + CLAUDE.md with doc-update rule | `scripts/pre-commit.sh`, `COMMIT_CHECKLIST.md`, `CLAUDE.md` | `d0ae35a` | 2026-04-09 11:35 PM ET |
| Cline | Add git push step to COMMIT_CHECKLIST.md | `COMMIT_CHECKLIST.md` | `b719c51` | 2026-04-09 11:39 PM ET |
| Cline | Add overnight automation: scripts/overnight_runner.sh + launchd plist (1am daily cron) | `scripts/overnight_runner.sh`, `scripts/com.cwn.overnight.plist`, `OVERNIGHT_TASKS.md` | `2563326` | 2026-04-09 11:43 PM ET |
| Cline | Add aider_plan.sh — one-shot full environment audit + multi-week plan generator | `scripts/aider_plan.sh` | `484248f` | 2026-04-09 11:44 PM ET |
| Cline | Fix news long-form post-reaction pause: 5 seconds → 3 seconds (server.js lines 5951, 5961, 5963) | `server.js` | `e9ae7f7` | 2026-04-09 11:47 PM ET |
| Cline | Fix overnight_runner.sh: add STATUS.md, OVERNIGHT_TASKS.md, MORNING_BRIEFING.md to Aider chat | `scripts/overnight_runner.sh`, `STATUS.md` | `afc0e7c` | 2026-04-10 8:52 AM ET |
| Claude Code | Config updates: Aider → Gemini 2.5 Pro (avoid TPM limit), remove hardcoded API key, Emily active, dashboard clips/streamer default 3→2 | `.aider.conf.yml`, `data/streamers.json`, `cwn_production.html`, `STATUS.md` | `e269afc` | 2026-04-10 9:15 AM ET |
| Cline | Add 🔄 REFRESH IDs button to dashboard job cards + refreshHeyGenIds() JS function; calls GET /heygen/latest-videos, matches by jobId_ prefix, updates segment URLs for Avatar V workaround | `cwn_production.html`, `server.js` | `ef5486a` | 2026-04-10 11:40 AM ET |
| Cline | Fix sendToHeyGen() title format: now sets HeyGen video title as `batchId_XX_SCENENAME` so REFRESH IDs can match by index and preserve segment order | `cwn_production.html` | `16ce6fe` | 2026-04-10 12:37 PM ET |
| Cline | Fix #1 Twitch clip mismatch (Gate 1 85/100): analyses mapped by streamer name (analysesByStreamer), items[].clips updated to match analysisClips order, generateClipAvailabilityReport() added | `server.js` | `3b5e9d0` | 2026-04-10 12:57 PM ET |
| Cline | Fix #2 clip report wiring: clipReportDataForQA declared + assigned inside twitch block; passed to both claudeScriptQA call sites (initial QA + post-claudeScriptFix QA) | `server.js` | `3b5e9d0` | 2026-04-10 2:09 PM ET |
| Cline | Add Node.js restart step to COMMIT_CHECKLIST.md (after push section) | `COMMIT_CHECKLIST.md` | `5e8a13c` | 2026-04-10 2:23 PM ET |
| Cline | Fix #3 ROOT CAUSE: claudeScriptQA() received 2D clipAnalyses array for Twitch; flatAnalyses() now iterates streamer×clip so attribution is always correct; NBA/News flat arrays unchanged | `server.js` | `64c2f70` | 2026-04-10 4:54 PM ET |
| Cline | Fix #4: Filter streamers with no real clip analyses before Gemini prompt — prevents hallucination for 0-clip streamers (only include streamers with ≥1 analysis > 50 chars) | `server.js` | `b5cac55` | 2026-04-10 5:13 PM ET |
| Cline | Fix #5: Explicit scene→clip mapping in Gemini prompt — each clip block now labels which SETUP/REACTION scenes it feeds, preventing Gemini from swapping clip 1 and clip 2 analyses | `server.js` | `84a780c` | 2026-04-10 5:28 PM ET |
| Cline | Fix #6A-E: Real video clip filtering (isVideoAnalyzed flag), dynamic report numbers, explicit CLIP ORDER swap in claudeScriptFix | `server.js`, `STATUS.md` | `67527e5` | 2026-04-10 5:47 PM ET |
| Cline | Add job persistence: persistedJobs loaded from data/jobs.json on startup, saveJobCard() writes to disk with 7-day pruning, GET /jobs endpoint for dashboard recovery after server restart | `server.js`, `STATUS.md` | `33a8800` | 2026-04-10 8:11 PM ET |
| Cline | Fix ASSEMBLE button missing after page refresh: add restoreJobsFromServer() + ↩ RESTORE JOBS button + auto-call on init (1.5s delay); reconstructs segments from server's persisted job cards so REFRESH IDs → ASSEMBLE flow works after any page reload | `cwn_production.html`, `STATUS.md` | `cfe2200` | 2026-04-10 9:30 PM ET |
| Cline | Add pipeline rollback + force-advance: POST /job/:id/rollback + POST /job/:id/advance + detectStage() in server.js; ↩ ROLLBACK + ⏭ FORCE ADVANCE buttons on every job card; rollbackJob() + advanceJob() JS functions call server then refresh queue | `server.js`, `cwn_production.html`, `STATUS.md` | `eac1073` | 2026-04-10 9:36 PM ET |
| Cline | Fix rollback/advance key mismatch: all 6 JSON responses now use `before`/`after` keys (dashboard JS reads resp.before/resp.after); 4 responses were incorrectly using `from`/`to` | `server.js` | `472718f` | 2026-04-10 9:41 PM ET |
| Cline | **Planned:** fix generateVideo() missing title (add `title: title \|\| undefined` to HeyGen API payload so REFRESH IDs can match videos by `batchId_XX_SCENENAME` prefix). **⚠️ NOT YET COMMITTED** — change is unstaged in `cwn_production.html`. The message on commit `6ce68c4` claims this fix but `git show 6ce68c4 --stat` shows zero HTML changes; see next row for what `6ce68c4` actually contains. | `cwn_production.html` (unstaged) | — | 2026-04-10 9:48 PM ET |
| Claude Code | Docs sync (committed as `6ce68c4` with misleading message due to concurrent-commit collision with Cline): wrote ROLLBACK_FORCE_ADVANCE_SPEC.md documenting shipped rollback/advance feature (stage machine, endpoint contracts, test checklist, open questions); added Job Persistence + Pipeline Controls + Zoom-to-Fill sections to CLAUDE.md; added `data/jobs.json` to .gitignore; archived two stale CLINE_HANDOFF_CLIP_MISMATCH_*.md to `docs/archive/`; deleted empty `scripts/trigger_assemble.js`; added `scripts/claude_consult.sh` (peer-agent tool). Added Tech Debt #5 (rollback/advance audit trail follow-up). | `ROLLBACK_FORCE_ADVANCE_SPEC.md`, `CLAUDE.md`, `.gitignore`, `STATUS.md`, `docs/archive/CLINE_HANDOFF_CLIP_MISMATCH_BUG.md`, `docs/archive/CLINE_HANDOFF_CLIP_MISMATCH_FIX_V2.md`, `scripts/claude_consult.sh` | `6ce68c4` (content) / see note | 2026-04-10 10:15 PM ET |
| Claude Code | Clarify commit history: commit `6ce68c4` message says "pass title to HeyGen API in generateVideo()" but actually contains Claude Code's docs-sync work (6 docs files, 0 lines of HTML). Concurrent commit activity mislabeled the commit. The real generateVideo() title fix is still unstaged in cwn_production.html (see row above). This STATUS entry exists so future agents reading git log aren't confused — the content is correct, only the message is wrong. | `STATUS.md` | `54650ed` | 2026-04-10 10:30 PM ET |
| Claude Code | Add Atomic Staging rule to COMMIT_CHECKLIST.md after concurrent-commit incident: documents the git-index race condition between multiple agents, the `git add && git commit` atomic pattern, practical guidance (chain with `&&`, explicit file lists, check reflog after committing), and a postmortem of the 2026-04-10 incident so future agents don't repeat it | `COMMIT_CHECKLIST.md`, `STATUS.md` | `54650ed` | 2026-04-10 10:45 PM ET |
| Cline | Fix REFRESH IDs for pre-title-format jobs: add `POST /heygen/video-urls` endpoint to server.js (batch video_id → URL lookup via HeyGen v1/video_status.get); add direct video_id fallback to `refreshHeyGenIds()` in dashboard — when prefix match returns 0, checks segments with videoId but no url, calls new endpoint, updates segment URLs + status. Fixes job `script_twitch_1775866928172` (sent before title fix) | `server.js`, `cwn_production.html`, `STATUS.md` | `12863ef` | 2026-04-10 10:00 PM ET |
| Claude Code | Document Upload-Post publish-time privacy policy in ROLLBACK_FORCE_ADVANCE_SPEC.md: new "Publish-Time Privacy (Rollback's Last Line of Defense)" section covering YT `private` / TikTok `SELF_ONLY` / IG account-wide private; Upload-Post API research (no IG draft flag, only `share_mode` with Trial Reels variants); current policy = IG account-wide private until 10 Reels shipped; exit criteria + Trial Reels API test curl; added Tech Debt #6 linking to the spec | `ROLLBACK_FORCE_ADVANCE_SPEC.md`, `STATUS.md` | pending | 2026-04-10 11:10 PM ET |
| Claude Code | Full diagnostic investigation + Cline handoff for broken Apr 10 Twitch long-form MP4: extracted 10+ frames across 5 reference videos (Apr 4-10) pinpointing pillarbox regression to Apr 10; downloaded raw HeyGen segment via API and ffprobe-verified 1920×1080 container with baked-in portrait pillarbox (HeyGen Avatar V glitch confirmed); root-caused "Error response" ticker bug to commit `b31533f` moving ticker HTMLs to `tools/` without updating `TICKER_MAP`; Rob identified new landscape-native 4K avatar `842f20b75ce242aea397f5030aa018aa`; wrote `CLINE_HANDOFF_AVATAR_AND_TICKER_FIX.md` with exact diffs for 4 fixes (avatar swap, OVERLAY_ZONE flip to top-right `x=1240`, LOGO_POS flip to top-left `x=20` to avoid collision, ticker `tools/` path fix); wrote `FUTURE_4K_MIGRATION_PLAN.md` parking doc for eventual 4K canvas migration (recommendation: stay at 1080p, benefit from supersampling); added corrections header to `CLAUDE_DIAGNOSIS_BROKEN_TWITCH_LONGFORM.md`. Cline owns all code edits; Claude Code wrote docs only. | `CLINE_HANDOFF_AVATAR_AND_TICKER_FIX.md`, `CLAUDE_DIAGNOSIS_BROKEN_TWITCH_LONGFORM.md`, `FUTURE_4K_MIGRATION_PLAN.md`, `STATUS.md` | pending | 2026-04-11 12:30 AM ET |
| Cline | **Avatar swap + overlay flip + ticker path fix (atomic):** swap portrait avatar `1a5d4e9130d2467fa01d9e1580aff829` → landscape-native 4K avatar `842f20b75ce242aea397f5030aa018aa` in `.env`, `cwn_production.html` (2 locations), `lib/config.js`; flip OVERLAY_ZONE x=40→x=1240 (TV card top-right, Bobby G faces viewer's left); flip LOGO_POS x=1780→x=20 (logo top-left to avoid collision); update 2 FFmpeg overlay burns in `server.js` (x=1240:y=40); update 2 logo overlay burns (overlay=20:20); fix TICKER_MAP paths: add `tools/` prefix to all 3 ticker HTML paths (broken since commit b31533f moved files without updating map); clean cached ticker MP4s + frames | `.env`, `cwn_production.html`, `lib/config.js`, `server.js`, `STATUS.md`, `CLAUDE.md` | `0d13fb0` | 2026-04-11 1:06 AM ET |
| Claude Code | **🚨 GATED SELF-HEALING PIPELINE ARCHITECTURE:** Wrote 3 foundational docs after Rob articulated 9 principles for autonomous pipeline fix loops. (1) `GATED_PIPELINE_ARCHITECTURE.md` — 900+ line authoritative spec: 9 principles (every gate has QA agent, programmatic fix path, no arbitrary retry limits, QA has authoritative decision power, every fix documented, specific diagnostics, collaborative QA dialogue), 7 stages with 7 gates (Gate 1 Claude + Gates 2-6 Gemini + Gate 7 Rob platform review), Gate Output Contract JSON schema, retry dialogue loop, save point strategy, loop detection, learning records (`gate_fixes.jsonl` + daily markdown), migration plan (Phases 1-8), rules of engagement for all agents. (2) `CLINE_HANDOFF_GATE2_SEGMENT_STRUCTURE.md` — 1200+ line ship-tonight Phase 1 handoff: root-cause analysis of parseSegments_v1 over-splitting CLIP_SETUP sections into 3 sub-segments (zombie fragments + duplicated labels), complete parseSegments_v2 single-segment-per-section implementation, A/B switch via `USE_PARSE_SEGMENTS_V2` flag, Gate 2 pure-code validator (6 checks: count/order/duplicates/empty/clip-url/clip-count), dialogue-based fix loop (`handleGate2Failure`), `POST /gate-fix-log` endpoint, dashboard UI spec. (3) `CLINE_HANDOFF_GATE1_CLIP_DIAGNOSTIC_UPGRADE.md` — Phase 2 handoff: upgrade Gate 1 clip availability report from generic "not in this episode" to 9 specific failure modes (TWITCH_API_EMPTY, STREAMER_NOT_FOUND, GQL_RESOLUTION_FAILED, CDN_DOWNLOAD_BLOCKED, GEMINI_ANALYSIS_TRUNCATED, etc.) each with cause + evidence + fix suggestion. Cline owns all code; Claude Code wrote docs only. | `GATED_PIPELINE_ARCHITECTURE.md`, `CLINE_HANDOFF_GATE2_SEGMENT_STRUCTURE.md`, `CLINE_HANDOFF_GATE1_CLIP_DIAGNOSTIC_UPGRADE.md`, `STATUS.md` | pending | 2026-04-11 3:15 AM ET |
| Cline | Phase 1 Gate 2: `parseSegments_v2` (A/B flag `USE_PARSE_SEGMENTS_V2`) + `gate2_validateSegmentStructure()` (6 checks: segment_count_mismatch, clip_count_mismatch, duplicate_labels, empty_avatar_segment, missing_clip_url, segment_order_mismatch) wired into `sendToHeyGen()` before confirm(); `handleGate2Failure()` fix loop + `displayGate2Pass()` UI; `POST /gate-fix-log` endpoint appends to `logs/gate_fixes.jsonl` | `cwn_production.html`, `server.js`, `STATUS.md` | `a1439b6` | 2026-04-11 2:47 AM ET |
| Claude Code | Wrote `CLINE_HANDOFF_GEMINI_CLIP_ANALYSIS_TRUNCATION_FIX.md` — surgical fix for Task #14: Gemini clip analysis truncation in `geminiAnalyzeClip()` at `server.js:5745` where `maxOutputTokens=500` cuts off the 4-section structured response, producing truncated output like `"Here's an analysis of the Twitch clip: 1. **Visually happening:**"` that causes Gate 1 CLIP MATCH to deduct 15 points (85/100, hard fail after 3 retries). Handoff includes: root-cause analysis with evidence from `output/qa_failures/gate1_script_fail_1775886258810.txt`, exact diffs for 4 changes (raise token cap 500→1500 + add finishReason detection + thumbnail fallback 200→500 + optional thumbnail finishReason mirror), test plan, rollback plan, teaching section on why 1500 is the right number, relationship to Phase 2 Gate 1 diagnostic upgrade handoff, commit message template, Cline checklist. ~30 min Cline work expected. Unblocks Gate 1 auto-pass on smoke tests (currently requires FORCE ADVANCE workaround). Prerequisite for the full Gate 1 diagnostic upgrade Phase 2 handoff to ship cleanly. | `CLINE_HANDOFF_GEMINI_CLIP_ANALYSIS_TRUNCATION_FIX.md`, `STATUS.md` | pending | 2026-04-11 7:30 AM ET |
| Cline | Fix Gate 1 hard fail (85/100 on all 3 retries): Gemini clip analysis `maxOutputTokens` raised 500→2000 in `geminiAnalyzeClip()` at `server.js:5745` — analyses were truncating to 54-71 chars, Claude couldn't verify clip match and deducted -15 every attempt | `server.js`, `STATUS.md` | `8929a47` | 2026-04-11 9:21 AM ET |
| Cline | ✅ Smoke test VERIFIED: Jason 2-clip Twitch long-form — Gate 1 scored **100/100** on attempt 1/3 (was 85/100 on all 3 retries before fix). Gemini analyses now 1154–1725 chars (was 54-71). All 7 scenes submitted to HeyGen. | — | — | 2026-04-11 9:31 AM ET |
| Cline | Fix ticker missing in assembled video: add `captureTicker(contentType)` pre-warm call in `startHeyGenPoller()` before `axios.post('/assemble')`. `TICKER_CACHE` is in-memory and cleared on server restart — ticker was always empty when auto-assembly fired after a restart. Pre-warm fires async (non-blocking) for long-form only. Fix clip ordering bug in `restoreJobsFromServer()`: was checking `isReaction` (matched both `CLIP_REACTION` and `REACTION`), inserting source clips BEFORE reaction scenes. Fixed to check `isSetup` and insert clips AFTER SETUP scenes, matching `startHeyGenPoller()` interleaving logic. Fixes: no ticker in assembled video + audio/scene mismatch (Bobby G talking about wrong clip). | `server.js`, `cwn_production.html` | `7016d6b` | 2026-04-11 9:58 AM ET |
| Cline | **Fix video freeze after source clip plays** (root cause confirmed via per-second bitrate analysis: 2700-5000 kbps during avatar segs, 5000 kbps during clip, then collapses to 200-700 kbps for remaining 90s). Root cause: xfade `filter_complex` offset math is broken when mixing avatar crossfades (0.3s) with clip hard cuts (0.001s) — `cumulativeDur` accumulates wrong offsets, causing FFmpeg to freeze on the last frame of the clip for the rest of the video. Fix: change `tsFiles.length > 30` condition to `tsFiles.length > 30 \|\| clipCount > 0` — always use concat demuxer (hard cuts everywhere, rock-solid reliable) when any source clips are present. xfade path still used for avatar-only jobs. | `server.js` | `6cd184a` | 2026-04-11 10:06 AM ET |
| Cline | **Fix Gate 3 false positive "OUTRO cut off"**: LATE sample window moved from `dur-25` to `dur-35` so the 20s clip captures the outro before it ends; `outroCutOff` regex tightened to only fire on explicit `OUTRO:.*FAIL` (not on "abrupt cut" language in passing reports). **Fix "Follow [streamer]. Link in description." appearing on every intro**: moved CTA from `[NAME]_INTRO` template to last `[NAME]_CLIP3_REACTION` only — CTA now appears exactly once per streamer, after their last clip reaction. | `server.js`, `STATUS.md` | pending | 2026-04-11 10:22 AM ET |
| Claude Code | **Spec reversal: Twitch intro card circle → TV rectangle (brand consistency)**. Rob flipped the design: all 3 content types (Twitch, NBA, News) now use the same 640×360 TV-rectangle for CWN brand consistency. Previously Twitch used a 720×840 canvas with circular profile PNG + text below while NBA/News used TV rectangles. Wrote `CLINE_HANDOFF_TWITCH_INTRO_CARD_TO_TV_DESIGN.md` (~1-2 hour Cline spec) with complete pseudocode for rewriting `generateIntroCardPNG()` at `server.js:500-670`, new `CONFIG.INTRO_CARD` keys (CANVAS 1280×720, IMAGE_SIZE 600, BORDER_WIDTH 10), Option A image-left/text-right layout, Option B image-top/text-bottom alternative, `/burn-streamer-intro` test plan, rollback plan, teaching section. Also fixed stale circle references in: `CLAUDE.md` (gotcha #6 + line 187), `GATED_PIPELINE_ARCHITECTURE.md` (Intro card design section — removed dual-design language, made TV rectangle authoritative, updated Gate 4 detection implications), `CLINE_HANDOFF_GATE2_SEGMENT_STRUCTURE.md` Part 9.5 (replaced "verify Twitch circle" with "TV migration is separate handoff"), `CLINE_HANDOFF_NBA_INTRO_CARD.md` (4 stale refs: `LEFT of Bobby G` → `TOP-RIGHT at x=1240`, "different visual designs" → "consistent TV rectangle"). Task #17 updated to reflect new migration target. Task #14 marked COMPLETED (Cline shipped `8929a47` while this was being written — raised maxOutputTokens 500→2000, smoke test verified Gate 1 100/100 per STATUS row 64). Cline owns all code; Claude Code wrote docs only. | `CLINE_HANDOFF_TWITCH_INTRO_CARD_TO_TV_DESIGN.md`, `CLAUDE.md`, `GATED_PIPELINE_ARCHITECTURE.md`, `CLINE_HANDOFF_GATE2_SEGMENT_STRUCTURE.md`, `CLINE_HANDOFF_NBA_INTRO_CARD.md`, `STATUS.md` | pending | 2026-04-11 10:35 AM ET |
| Cline | Fix 2 dashboard bugs: (1) **Old cleared jobs reappearing** — `clearAllJobs()` and `clearDone()` now call `DELETE /job/:id` for each job before clearing local JOBS array, so server removes them from `data/jobs.json` and they don't reappear on next `restoreJobsFromServer()`. (2) **Dashboard shows "0/9 rendering" after page refresh when FFmpeg already ran** — `restoreJobsFromServer()` now checks `serverJob.assembledAt` and `serverJob.stage` before restoring; assembled/published jobs get `status='completed'` and all avatar segments marked `status='completed'` so ASSEMBLE button (not "0/9 rendering") appears after any page reload. | `cwn_production.html`, `STATUS.md` | pending | 2026-04-11 11:32 AM ET |
| Cline | **Fix auto-assembly output filename (was raw jobId slug)**: `startHeyGenPoller()` was passing `jobTitle: jobId` to `/assemble`, producing filenames like `script_twitch_1775920714320_1775920965775.mp4`. Now builds `_humanTitle` from `card.savedAt` + `contentType` + avatar/clip counts — e.g. `"TWITCH Saturday, April 11, 2026 (7 avatar + 2 clips)"` — matching the human-readable format the dashboard's `assembleJob()` already used. | `server.js`, `STATUS.md` | `09f9502` | 2026-04-11 1:49 PM ET |
| Cline | **Fix ticker missing in auto-assembled video + Twitch intro card circle→TV rectangle migration**: (1) Ticker fix: `startHeyGenPoller()` was calling `captureTicker().then(...)` fire-and-forget then immediately POSTing to `/assemble` — ticker capture takes 2-3 min so `TICKER_CACHE` was always empty when FFmpeg reached Step 6. Fixed to `await captureTicker(contentType)` before assembly POST. (2) Intro card migration: rewrote `generateIntroCardPNG()` from 720×840 circle design → 1280×720 TV rectangle (dark slate `#1a1a1a` bg, image-left 600×600 rounded at x=60, text-right column at x=740: name gold 136px, origin white 88px, fact grey italic 64px word-wrapped, gold 10px border); updated `lib/config.js` `CONFIG.INTRO_CARD` keys (removed CIRCLE_* keys, added IMAGE_SIZE/IMAGE_MARGIN/TEXT_GAP/BORDER_WIDTH/DURATION_SECONDS). All 3 content types now use consistent 640×360 TV-rectangle design. | `server.js`, `lib/config.js`, `STATUS.md` | `6028820` | 2026-04-11 2:10 PM ET |
| Cline | **Fix dashboard persistence after auto-assembly**: `startHeyGenPoller()` only set `autoAssembledAt` at POST-fire time — `assembledAt`, `stage`, `outputPath`, `finalUrl` were never written to `persistedJobs`. Added in-process `pollAssemblyCompletion` closure that watches `assemblyJobs[assemblyId]` every 15s (same Node process, no HTTP) and calls `saveJobCard()` when status is `done`/`manual_review`, persisting all assembly completion data including Gate 5 QA scores and Drive URL. Dashboard now reflects completed assembly state after auto-assembly fires. | `server.js`, `STATUS.md` | pending | 2026-04-11 2:58 PM ET |
| Cline | **feat(layout): TV card 640×360→840×472, logo repositioned, ticker 64→72, config-driven FFmpeg filters**: Rob approved dimensions via `tmp/preview/FINAL_c2_ticker72.jpg`. TV card: OVERLAY_ZONE `{x:1240,y:40,w:640,h:360}`→`{x:1040,y:100,w:840,h:472}` (31% larger, 16:9, clears Bobby G shoulder). Logo: LOGO_POS `{x:20,y:20,size:120}`→`{x:80,y:10,size:100}` (clears mic arm). Ticker: HEIGHT 64→72 (+12.5%). All 5 FFmpeg filter locations in `server.js` now read from CONFIG instead of hardcoded values (lines 3754, 3904, 4207, 4217, 4295, 9589). | `lib/config.js`, `server.js`, `STATUS.md` | pending | 2026-04-11 3:57 PM ET |
| Claude Code | **Layout dimension fixes handoff + 5-handoff confirmation checklist**: Wrote `CLINE_HANDOFF_LAYOUT_DIMENSIONS_AND_HANDOFF_CONFIRMATION.md` after 3rd smoke test visual review. Rob approved the video content (Gate 1 100/100, full-bleed avatar, TV card/logo/ticker all in correct positions) but flagged 3 dimensional tweaks: (1) TV card rendering at 360×203 instead of target 640×360 because `server.js:3754 + 3904` hardcode `scale=360:-1` which calculates height from the new 1280×720 canvas — fix: read from `CONFIG.VISUAL_LAYOUTS.LONG_FORM.OVERLAY_ZONE`, new dimensions 840×472 at (1040, 100) which is 31% larger for NBA scores/news headlines readability. (2) Logo at (20,20,120) overlaps the new avatar's microphone arm — fix: move to (80, 10, 100) up-and-right, slightly smaller. (3) Ticker 64px feels small — fix: bump to 72px (+12.5%), applied uniformly to twitch/nba/news via shared `CONFIG.TICKER`. Plus: replace hardcoded FFmpeg scale/overlay values with config-driven reads at server.js:3754, 3904, 3957, 4037, 9308 so future dimension changes are single config edits. Also wrote Part 5 handoff confirmation checklist asking Cline to confirm commit hashes for handoffs 1-4 (avatar/ticker path fix `0d13fb0`, Gate 2 `a1439b6`, Gemini truncation `8929a47`, Twitch TV migration pending) so we can archive shipped docs. Rob approved the dimensional changes via `tmp/preview/FINAL_c2_ticker72.jpg` drawbox preview. Cline owns code; Claude Code wrote docs only. | `CLINE_HANDOFF_LAYOUT_DIMENSIONS_AND_HANDOFF_CONFIRMATION.md`, `STATUS.md` | pending | 2026-04-11 3:45 PM ET |
| Claude Code | Wrote `CLINE_DIAGNOSTIC_TICKER_MISSING_SMOKE_TEST_2.md` after 2nd smoke test visually confirmed missing ticker — race condition analysis (pre-warm fires async in `startHeyGenPoller()`, assembly reaches ticker bake step before Puppeteer finishes, `TICKER_CACHE` object not yet populated in-memory, cache check returns null, ticker skipped) with 3 recommended fix options. Option A (`await captureTicker()` before assembly POST) recommended as simplest. **Cline shipped Option A independently** while diagnostic was being written — timing was ~20 min before Claude Code's commit. Doc marked SUPERSEDED and preserved as historical record. Task #19 (missing ticker) and Task #17 (Twitch circle→TV migration) both marked COMPLETED. Cline owns code; Claude Code wrote docs only. | `CLINE_DIAGNOSTIC_TICKER_MISSING_SMOKE_TEST_2.md`, `STATUS.md` | pending | 2026-04-11 2:30 PM ET |
| Claude Code | **Archive 5 shipped handoffs to `docs/archive/`**. Cline confirmed commit hashes for handoffs 1-4 in Part 5 of `CLINE_HANDOFF_LAYOUT_DIMENSIONS_AND_HANDOFF_CONFIRMATION.md` (see commit `f4b5577`). Moved to archive: `CLINE_HANDOFF_AVATAR_AND_TICKER_FIX.md` (`0d13fb0`), `CLINE_HANDOFF_GATE2_SEGMENT_STRUCTURE.md` (`a1439b6`), `CLINE_HANDOFF_GEMINI_CLIP_ANALYSIS_TRUNCATION_FIX.md` (`8929a47`), `CLINE_HANDOFF_TWITCH_INTRO_CARD_TO_TV_DESIGN.md` (`6028820`), `CLINE_HANDOFF_NBA_INTRO_CARD.md` (verified `/nba/generate-intro-card` endpoint exists at `server.js:10576`, STATUS.md "What's Working" already lists it). Remaining in repo root: `CLINE_HANDOFF_GATE1_CLIP_DIAGNOSTIC_UPGRADE.md` (Phase 2 pending, not yet shipped) and `CLINE_HANDOFF_LAYOUT_DIMENSIONS_AND_HANDOFF_CONFIRMATION.md` (kept active until 4th smoke test confirms the dimension fixes worked visually). Layout dimensions also shipped by Cline as `33ed559` — TV card 840×472, logo (80,10,100), ticker 72px, config-driven FFmpeg filters. Docs-only commit. | `STATUS.md`, `docs/archive/CLINE_HANDOFF_AVATAR_AND_TICKER_FIX.md`, `docs/archive/CLINE_HANDOFF_GATE2_SEGMENT_STRUCTURE.md`, `docs/archive/CLINE_HANDOFF_GEMINI_CLIP_ANALYSIS_TRUNCATION_FIX.md`, `docs/archive/CLINE_HANDOFF_TWITCH_INTRO_CARD_TO_TV_DESIGN.md`, `docs/archive/CLINE_HANDOFF_NBA_INTRO_CARD.md` | `699b4cb` | 2026-04-11 4:15 PM ET |
| Claude Code | **TV card reposition to exact 16:9 + ticker FPS fix**: 4th smoke test visually showed TV card left edge overlapping Bobby G's right shoulder. Rob derived the aspect ratio math and picked Option (c): `{x:1160, y:100, w:720, h:405}` — mathematically exact 16:9 (720÷405 = 1.7778), 720 width is standard HD building block (no FFmpeg sub-pixel rounding on the 405 height since it's exact `720 × 9/16`), right edge at 1880 (40px margin), clears Bobby G's shoulder completely. Ticker also visibly shaky due to FPS mismatch (ticker 15fps, video 30fps → "hold and jump" stutter). Quick fix: bump `CONFIG.TICKER.FPS: 15 → 30` + cache bust `rm tmp/ticker_*.mp4 && rm -rf tmp/ticker_frames_*`. Expected ~70% reduction in visible shakiness. Long-term fix parked as Part 3 of the handoff: replace Puppeteer pre-rendered MP4 with FFmpeg `drawtext` filter for smooth integer-math scrolling (~4 hours Cline work, queued for post-12-test-suite). Also logged Rob's decision to move task tracking from in-session TaskCreate → Jira + Confluence stack (Rob has years of Atlassian experience). Migration plan writing parked until pipeline stable. Handoff doc: `CLINE_HANDOFF_TICKER_STABILITY_TV_REPOSITION.md`. Cline owns code; Claude Code wrote docs only. | `CLINE_HANDOFF_TICKER_STABILITY_TV_REPOSITION.md`, `STATUS.md` | pending | 2026-04-11 5:15 PM ET |

| Cline | **fix(layout): TV card 840×472→720×405 exact 16:9 + ticker FPS 15→30 for smoother scroll**: Rob reviewed 4th smoke test — TV card left edge at x=1040 overlapped Bobby G shoulder; Option (c) {x:1160,y:100,w:720,h:405} is mathematically exact 16:9 (720÷405=1.7778), right edge at 1880 (40px margin). Ticker FPS 15→30 eliminates hold-and-jump stutter (~70% of visible shake). Long-term drawtext replacement parked as Task #22 (post-12-test). server.js unchanged — config-driven filters from 33ed559 auto-propagate. | `lib/config.js`, `STATUS.md` | `0b613af` | 2026-04-11 4:54 PM ET |
| Claude Code | **Expand Aider overnight queue with INDEPENDENT tasks + narrow pause banner**: After Rob confirmed Jira+Confluence as the task tracking stack (will handle Atlassian signup himself), Rob asked whether Aider could do more than nightly server.js module split. Answer: yes, plenty. Updated `OVERNIGHT_TASKS.md`: (1) narrowed the pause banner from "all of server.js blocked" to just "coordinate atomic staging" — most tasks open; (2) added new "🟢 INDEPENDENT — Safe Any Night" section with 10 new tasks that don't touch server.js or cwn_production.html: unit tests for parseSegments_v2 + Gate 2 validator, `/health` endpoint as new `lib/routes/health.js` module, `/metrics` endpoint (Prometheus format) as new `lib/routes/metrics.js`, qa_failures rotation script, nightly Drive backup of `data/*.json`, errors catalog doc generation from `logs/errors.jsonl`, Jira sync script (BLOCKED until Rob sets up Atlassian), `cleanAvatarText` extraction to `lib/text_cleanup.js`, markdown link linter, style guide refinement. Each task has file, what, why, how, risk, estimate, dependency. Also documented the three-layer Jira sync architecture (Atlassian native GitHub app handles 90% / deterministic cron script handles 9% / agent-driven manual handles 1%). Cline owns code; Claude Code wrote docs only. | `OVERNIGHT_TASKS.md`, `STATUS.md` | pending | 2026-04-11 7:05 PM ET |
| Cline | **fix: TV card y=100→y=352 — exits neon-map billboard zone, preserves ticker clearance**: Smoke test showed card at y=100 sitting inside the neon world map behind Bobby G. Halfway drop (100+603)/2=352 moves it to quieter bookshelf zone; bottom edge at y=757 leaves 251px above ticker at y=1008. x/w/h unchanged. server.js unchanged (config-driven). | `lib/config.js`, `STATUS.md` | `2781d8a` | 2026-04-11 5:53 PM ET |
| Cline | **feat(dashboard): 12-streamer silent-drop visibility** — `generateTwitch()` now tracks every streamer that returns 0 valid clips and surfaces the reason in the status bar before script generation. Changes: (1) `droppedStreamers[]` array init after `var items = []`; (2) user-not-found branch pushes to droppedStreamers; (3) 7-day clip window (was 24h — too narrow for less-active streamers); (4) `extraemily` + `yourragegaming` added to DISPLAY_NAMES; (5) else branch after `if(validClips.length)` pushes reason ('No clips in 7d window' or 'All N clips had invalid titles'); (6) `xC.onerror/xC.ontimeout` push 'Twitch clips API timeout/error'; (7) `xU.onerror/xU.ontimeout` push 'Twitch users API timeout/error'; (8) `check()` surfaces drop summary via `setGeneratingState('twitch-gen-status', dropMsg)` before `callFullScriptServer`. | `cwn_production.html`, `STATUS.md` | `cf3868b` | 2026-04-11 5:57 PM ET |
| Cline | **fix(auto-publish): thumbnail + pinned comment + chapters in description** — Gate 6 auto-publish block in `/assemble` endpoint now: (1) uploads extracted 15s thumb frame to Drive (`thumbDriveUrl`) and forwards as `thumbnailUrl` to `/publish`; (2) adds `PINNED_COMMENT_TEMPLATES` map (twitch/nba/news) — fixed per-content-type first comment overriding Claude's freestyle; (3) `buildYouTubeChapters()` helper builds `0:00 Intro\n0:30 Scene…` from `req.body.segments[]` + `assemblyJobs[asmId].segmentDurations[]`; (4) `descriptionWithChapters` appends `\n\n⏱ CHAPTERS\n{chapterText}` to description before publish call. | `server.js`, `STATUS.md` | `61f15a7` | 2026-04-11 6:29 PM ET |
| Cline | **feat(heygen): real pauses via SSML + sync ticker capture to config height** — (1) `server.js:1856` adds `input_type: 'ssml'` to HeyGen v2/video/generate voice payload — opts in to SSML parsing so `<break>` tags are honored. (2) `cwn_production.html:3235` `cleanAvatarText()` now replaces `[beat]` with `<break time="500ms"/>` instead of `...` — HeyGen TTS treats ellipsis as elidable punctuation (no pause), SSML break gives a precise 500ms pause. (3) `server.js:4982` `captureTicker()` HEIGHT constant now reads `CONFIG.TICKER.HEIGHT` (72) instead of hardcoded 64 — fixes 8px gap at frame bottom where ticker PNG was shorter than overlay position expected. Ticker cache cleared (`rm tmp/ticker_*.mp4`). Gate 1 BEAT PLACEMENT checks unchanged — Gemini still writes `[beat]`, Gate 1 still enforces; this only affects the last step before HeyGen render. | `server.js`, `cwn_production.html`, `STATUS.md` | `ba87ec5` | 2026-04-11 8:17 PM ET |
| Cline | **fix(layout): TV card 720×405→520×293 at (1360, 60) — broadcast over-the-shoulder style**: Previous position (1160, 352) overlapped Bobby G's shoulder/upper chest. New 520×293 card (exact 16:9: 520÷293=1.7748) sits top-right next to his head — 60px top padding, 40px right margin (1360+520=1880), broadcast over-the-shoulder style. Derived from Gemini measurement script + Rob's visual review. `lib/config.js:55` only — server.js unchanged (config-driven filters auto-propagate). | `lib/config.js`, `STATUS.md` | `e69ded7` | 2026-04-11 8:49 PM ET |
| Cline | **feat(heygen): `<break>` 500ms→1000ms + `dynamic_duration: true` for SSML pause unlock** — (1) `cwn_production.html:3235` `cleanAvatarText()` bumps `[beat]` replacement from `<break time="500ms"/>` → `<break time="1000ms"/>` (HeyGen documented max reliable break is 1s; 500ms was too short to be audible). (2) `server.js:1866` adds `dynamic_duration: true` to HeyGen v2/video/generate requestBody (between `dimension` and `test: false`) — auto-adjusts video length to match audio including SSML breaks so HeyGen doesn't auto-trim silence. **Known risk:** if HeyGen V2 rejects `dynamic_duration` as an unknown field, rollback = remove that one line, keep 1000ms bump, push a second commit. | `cwn_production.html`, `server.js`, `STATUS.md` | pending | 2026-04-11 8:52 PM ET |
| Cline | **Fix 5 — fix(news): /newscast-overlay route path — tools/ prefix missing (server.js:1300)**: Commit b31533f moved clipzworld_newscast.html into tools/ but server.js:1300 kept pointing at repo root. res.sendFile threw ENOENT → Express HTTP 500 → Puppeteer screenshotted blank PNG → FFmpeg blended blank over Bobby G → invisible newscast overlay for every News run since Apr 11. Same class of bug as ticker path fix in 0d13fb0. Fix: add tools/ prefix. Verified: curl returns HTTP:200 SIZE:15964. | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | `971429d` | 2026-04-12 6:00 PM ET |
| Cline | **Fix 6 — fix(news): rewrite Gemini prompt to eliminate INTRO/SETUP repetition + rename CLIP_REACTION→SUMMARY (server.js:6685-6737)**: Rob reviewed Gate 1 script from smoke test #1 and flagged 4-beat redundancy: INTRO delivers headline+context, SETUP restated it, CLIP_REACTION+REACTION produced two unconnected quips. Root cause in News Gemini prompt. Changes: (1) SETUP rewritten to EXACTLY 1 sentence — new fact or hook, not a restatement of INTRO; (2) CLIP_REACTION renamed to SUMMARY with new job: 1-2 sentences factual recap of what just played, no opinions/reactions/quips; (3) REACTION tightened: deadpan take only, must not recap (that's SUMMARY's job); (4) word count target 80-120 → 100-140 per story; (5) Gate 1 QA comment + sceneHeaders push updated to STORY1_SUMMARY. Scene count unchanged (42 for 10 stories). No downstream code changes (parseSegments_v2, heygen-poller, assembly all unchanged). | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | `9a4fcc6` | 2026-04-12 6:30 PM ET |
| Cline | **Fix 7 — fix(news): newscast overlay RGBA alpha + logo position** — Complete rewrite of `tools/clipzworld_newscast.html` (design-intent comment block, `.lower-third` default hidden, `.lower-third.visible` slideInLeft, `body{background:transparent}` kept). `lib/config.js`: added `LOGO_POS_NEWS: {x:1725, y:910, size:90, opacity:0.85}` (News-specific logo on Bobby G's coffee mug). `server.js` 3 changes: (1) `generateNewscastOverlay()` rewritten — `omitBackground:true` in `page.screenshot()` (CRITICAL: without it `body{background:transparent}` composites against white canvas → rgb24 PNG with YAVG~213 instead of real RGBA); `page.evaluate()` toggles `.lower-third.visible` class per segment. (2) Burn loop: condition changed from `isIntro && contentType==='news'` → `contentType==='news' && segType==='avatar'`; `isStoryIntro` two-state burn generates overlayVisible+overlayHidden PNGs and uses `overlay=0:0:enable='lte(t,introDur)'` + `overlay=0:0:enable='gt(t,introDur)'` (replaces broken `blend=all_mode=normal:all_opacity=1` which doesn't composite RGBA alpha correctly); non-INTRO avatar segments use single-state overlayHidden burn. (3) Logo burn: both main assembly (line 4390) and remediate path (line 9817) now branch on `contentType==='news'` to use `LOGO_POS_NEWS` instead of `LOGO_POS`. Grep verified: `omitBackground` at lines 3830+10477+10479; `overlay=0:0` at lines 3829+3889+3890+3896+3936; `LOGO_POS_NEWS` at lines 4390+9817. `node -c server.js` exit 0. | `tools/clipzworld_newscast.html`, `lib/config.js`, `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | `4a2ac67` | 2026-04-12 9:23 PM ET |
| Cline | **Fix 8B — fix(news): Build News TV card (og:image scrape + OVERLAY_ZONE burn)** — All 6 pieces implemented: (1) `scrapeArticleOgImage()` helper — axios GET + cheerio parse, extracts `<meta property="og:image">` from Al Jazeera article URLs; (2) News analysis block — runs og:image scraping in parallel with Gemini analysis, attaches `item.heroImageUrl` to each story; (3) Job card save — persists `heroImageUrl` in `newsItems[]` array so heygen-poller survives server restart; (4) heygen-poller cardData — extends cardData for `STORY#_INTRO` segments to include `heroImageUrl`; (5) `generateNewsStoryCardPNG(storyData, outputPath)` — Canvas renderer at 2× resolution (1040×586 for OVERLAY_ZONE 520×293): hero image left-half, dark right panel with gold border, headline word-wrapped, source tag, category badge; (6) Second FFmpeg overlay burn inside existing Fix 7 `isStoryIntro` block — runs AFTER chrome burn completes using `inputForTS` (already points at chrome-burned path), `enable='lte(t,introDur)'` time-gating, non-fatal on failure so chrome burn still applies. `node -c server.js` → SYNTAX OK. Root cause of "no TV card" across all News smoke tests Apr 7–12: archived handoff asserted it was "already working" but `generateNewsStoryCardPNG` never existed in code. | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | pending | 2026-04-12 10:27 PM ET |
| Cline | **Fix 9 — fix(news): Wire Al Jazeera video scraping via JSON-LD + yt-dlp (unblocks mid-story clips)**: New `scrapeArticleVideo(articleUrl)` helper — fetches article HTML, extracts JSON-LD `VideoObject.embedUrl` (Brightcove player URL), runs `yt-dlp --dump-json` on the embed URL (not the article URL — yt-dlp rejects article URLs as "Unsupported URL" but succeeds 100% on Brightcove embed URLs), filters live streams (`is_live=true`, `duration=0`, URL contains `thehlive.com`), returns HLS manifest URL. Wired into News analysis block alongside `scrapeArticleOgImage` and `geminiAnalyzeClip` via `Promise.all` — `scrapedVideoUrls[i]` assigned to `item.videoUrl` when non-null, which flows through Fix 1's `orderedClipUrls` build automatically. Hit rate: 40% on mixed RSS feed (3/10 on-demand clips + 1 live stream filtered), 100% on `/video/` path articles. `node -c server.js` → exit 0. | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | pending | 2026-04-12 11:51 PM ET |
| Cline | **NBA Wave 1+2 — fix(nba): Gate 1 QA + expectedScenes updated for 3-scene pattern (NARRATION replaces SETUP+CLIP_REACTION)**: 8 targeted changes to `server.js` eliminating all references to the old 4-scene NBA pattern (GAME#_INTRO + GAME#_SETUP + GAME#_CLIP_REACTION + GAME#_REACTION) and replacing with the correct 3-scene pattern (GAME#_INTRO + GAME#_NARRATION + GAME#_REACTION). Changes: (1) `claudeScriptQA()` NBA checklist comment updated to 3-scene pattern; (2) `claudeScriptQA()` `expectedScenes` NBA: `* 4` → `* 3`; (3) `geminiScriptQA()` `expectedScenes` NBA: `* 4` → `* 3`; (4) `claudeScriptQA()` checklist item 6: GAME SETUP → NARRATION; (5) `claudeScriptQA()` checklist item 10: word count → per-game NARRATION targets; (6) `geminiScriptQA()` checklist item 5: GAME SETUP → NARRATION; (7) `geminiScriptQA()` checklist item 9: word count → per-game NARRATION targets; (8) `generate-full-script` endpoint `expectedScenes` NBA: `* 4` → `* 3`. Grep verified: 0 CLIP_REACTION hits in NBA QA/prompt blocks; 15+ NARRATION hits across Gate 1 QA (lines 2347, 2353, 2357, 2654, 2694, 2698) and NBA prompt block (lines 6912–7266). `node -c server.js` → exit 0. | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | pending | 2026-04-13 12:18 AM ET |
| Cline | **Gap #51 — fix(news): remove [3-second pause] stage direction from Gemini prompt + defensive cleanAvatarText in generateVideo()**: Root cause: Gemini prompt VALIDATION CHECKLIST had 3 references instructing it to write `[3-second pause — hold on source clip]` into every STORY#_REACTION scene. HeyGen renders the bracket text as burned-in on-screen text, causing Gate 3 to fail smoke test #6 three times. Root cause fix (server.js): (1) removed `+ [3-second pause — hold on source clip]` from REACTION scene rule; (2) replaced `Add "[3-second pause — hold on source clip]" before moving to next story` with `Between stories, the assembly layer will add a 3-second hold on the source clip before cutting to the next story. Do NOT write stage directions in the script — just end the REACTION scene with a single deadpan sentence.` Defensive fix (cwn_production.html): wrapped `script` in `cleanAvatarText()` inside `generateVideo()`'s HeyGen payload — `applyPronunciations(cleanAvatarText(script))` — so any bracket directives that slip through the prompt are stripped before reaching HeyGen API. `node -c server.js` → exit 0. | `server.js`, `cwn_production.html`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | `d5c53ea` | 2026-04-13 7:30 AM ET |
| Cline | **Fix 8 (smoke test #7) — fix(gate2): Gate 2 score regex handles bracketed numbers + prompt uses angle brackets**: Root cause: Gate 2 Gemini prompt used `[number from 0-100]` bracket notation in the `OVERALL SCORE` field. Gemini mimics the format and outputs `OVERALL SCORE: [85]` with literal brackets. The `segScore` regex `/OVERALL SCORE:\s*(\d+)/i` fails to match — every segment defaulted to 80/100, causing every smoke test to stall at MANUAL_REVIEW even when real Gemini scores were 95-98. Fix A (server.js:2991): regex updated to `/OVERALL SCORE:\s*\[?(\d+)\]?/i` — optional brackets. Fix B (server.js:2970): prompt placeholder changed from `[number from 0-100]` to `<number from 0-100>` to discourage future bracket copying. `node -c server.js` → SYNTAX OK. | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | pending | 2026-04-13 10:40 AM ET |
| Cline | **Fix 10 (smoke test #7) — fix(nba): NBA TV card canvas 720x840->1040x586 landscape layout**: Root cause: generateGameStoryCardPNG() used 720x840 portrait canvas (6:7 ratio) but OVERLAY_ZONE is 520x293 (16:9 landscape). FFmpeg lanczos scale squished the portrait card into landscape, distorting all text and images. Fix: rewrite canvas to 1040x586 (exact 2x pixel-doubled OVERLAY_ZONE). Layout changed from portrait (center image, text below) to landscape (image left-half at 42%W x 78%H, text right column starting at 44%W). Gold border added around entire card. Title font Math.round(H*0.1) (~59px), subtitle Math.round(H*0.075) (~44px), text starts at Math.round(H*0.28) from top. All pixel values proportional to W/H. node -c server.js -> exit 0. Rob-approved exception to News-only scope rule. | server.js, STATUS.md, LONGFORM_FIX_ROTATION.md | pending | 2026-04-13 10:42 AM ET |
| Cline | **Fix 7 + Fix 4 (smoke test #7) — fix(news): LIVE indicator margin-right:80px + flag flush left (clipzworld_newscast.html)**: Fix 7: added margin-right:80px to .top-right CSS rule — pulls LIVE indicator + date inward from right frame edge. Fix 4: .lower-third CSS — removed slide-in animation from .visible state, added explicit transform:translateX(0) and margin-left:0 to ensure flag leading edge is flush at x=0 of frame (no 8px inset). Both are single-line CSS changes in tools/clipzworld_newscast.html. | tools/clipzworld_newscast.html, STATUS.md, LONGFORM_FIX_ROTATION.md | pending | 2026-04-13 10:44 AM ET |
| Cline | **Fix 9b — fix(downloadFile): allow Brightcove CDN + handle HLS manifests via FFmpeg**: Fix 9's `scrapeArticleVideo()` returns Brightcove HLS manifest URLs (e.g. `https://manifest.prod.boltdns.net/manifest/v1/hls/v3/clear/...`) but `downloadFile()` blocked them at the SSRF whitelist with "URL blocked: not from trusted domain". Even if whitelisted, naive axios streaming downloads the ~2KB text manifest, not the actual video segments. Two changes to `downloadFile()` in `server.js`: (1) Added Brightcove + Al Jazeera domains to `trustedDomains[]`: `boltdns.net`, `brightcove.net`, `brightcove.com`, `edge.api.brightcove.com`, `aljazeera.com`, `aljazeera.net`. (2) HLS detection branch: `/\.m3u8(\?|$)/i.test(url) \|\| /\/hls\//i.test(url)` routes to `execFile(ffmpegPath(), ['-i', url, '-c', 'copy', '-bsf:a', 'aac_adtstoasc', '-movflags', '+faststart', '-y', destPath])` with 120s timeout — FFmpeg resolves all HLS segments and muxes to MP4. Axios streaming path unchanged for non-HLS URLs. `node -c server.js` → exit 0. | `server.js`, `STATUS.md`, `LONGFORM_FIX_ROTATION.md` | pending | 2026-04-13 7:33 AM ET |
---

## 📊 Phase Progress

| Phase | Owner | Status | Commit |
|-------|-------|--------|--------|
| Phase 1: Thumbnail Updates | Claude Code | ✅ COMPLETE | `767aecf` |
| Phase 2: Short-Form Infrastructure | Cline | ✅ COMPLETE | `88e20eb` |
| Phase 3: Caption & Prioritization | Aider | ✅ COMPLETE | `186ea3d` |
| Phase 4: Operations Fixes | Cline | ✅ COMPLETE | `9fa9340` |
| Phase 5: Creative Layer | Claude Code + Rob | ✅ COMPLETE | `b3fcdff` |
| Phase 6: Publish Integration | Cline | 🟡 In Progress — ready to test | — |

---

## ✅ What's Working (Full System)

- **Full pipeline:** Script Gen → Gate 1 → HeyGen → Gate 2 → Assembly → Gate 3 → Drive Upload → Publish
- **All 3 content types:** Twitch, NBA, News (long-form + short-form)
- **Scene headers:** spaces → underscores (no more broken Gemini parsing)
- **Multi-platform publish:** YouTube, TikTok, Instagram via Upload-Post API (`server.js:6782`)
- **Short-form captions:** `generateShortFormCaption()` — 90-150 char + hashtags + altText
- **News prioritization:** `prioritizeNewsStories()` — 18 urgency keywords, score-sorted
- **Thumbnails:** FFmpeg-based for all 3 content types, episode auto-increment — tagline "BECAUSE THE LIGHT WAS ON" ✅
- **NBA intro card:** `/nba/generate-intro-card` — Puppeteer → 640×360 PNG (`server.js:9800`) ✅
- **News intro card:** `/news/generate-intro-card` — OG image scraper → 640×360 (`server.js:4615`) ✅
- **TV card position:** All 3 overlay positions (OVERLAY_ZONE + 2 FFmpeg burns) → `x=1240` top-right (Bobby G faces viewer's left; logo moved to top-left `20:20` to avoid collision) ✅
- **Split-screen short-form:** `/capcut/split-screen` + `assembleShortForm()` — 9:16 portrait (`server.js:8358`) ✅
- **CapCut routes:** `/capcut/init`, `/capcut/add-segment`, `/capcut/ticker`, `/capcut/logo`, `/capcut/finalize`
- **Safety zone check:** TikTok + Reels AABB+circle overlap validation
- **Gate 3/6 approval flow:** Human checkpoint → approve → auto-publish with status polling
- **Disk usage + cleanup:** `GET /disk-usage`, `POST /cleanup`
- **VectCutClient:** `assembleShortForm()`, `addBrandedOverlay()`, `healthCheck()`
- **Upload status tracking:** `GET /upload-status/:trackingId` + `data/upload_status.json`
- **Avatar V hybrid workflow:** 🔄 REFRESH IDs button in dashboard syncs manually-upgraded HeyGen videos (`GET /heygen/latest-videos`) ✅

---

## 🎬 Avatar V Production Workflow (Current Standard)

> HeyGen Avatar V is **web-console only** — no API access yet. Use this hybrid workflow for all productions until the API is released.

### Step-by-step

| Step | Where | Action | Automated? |
|------|-------|--------|-----------|
| 1 | CWN Dashboard | Generate script → click **SEND TO HEYGEN** | ✅ Auto |
| 2 | HeyGen web UI | Wait for all segments to finish rendering (green ✓) | 👤 Manual wait |
| 3 | HeyGen web UI | For each segment: click **Regenerate** → switch to **Avatar V** → confirm. Repeat for all segments | 👤 Manual |
| 4 | CWN Dashboard | Click **🔄 REFRESH IDs** on the job card | 👤 One click |
| 5 | CWN Dashboard | Click **⚙ ASSEMBLE** | 👤 One click |
| 6 | Background | FFmpeg downloads all segments, stitches, adds ticker/logo/overlays | ✅ Auto |
| 7 | Background | Gate 5: Gemini visual QA on assembled video | ✅ Auto |
| 8 | CWN Dashboard | Gate 3: Review Gate 5 score → click **✅ APPROVE & UPLOAD →** | 👤 One click |
| 9 | Background | Upload-Post publishes to YouTube + TikTok + Instagram as **private drafts** | ✅ Auto |
| 10 | YouTube / TikTok / IG | Review private video → flip to **public** when ready | 👤 Manual |

### Notes
- **REFRESH IDs** matches by title prefix `jobId_XX_SCENENAME` — only works for jobs sent after Apr 10 2026 (when title format was added to `sendScriptToHeyGen()`)
- Older jobs without titles: paste URLs manually into segment fields, or re-send
- When Avatar V API launches: swap `HEYGEN_AVATAR_ID` in `.env` — zero code changes needed
- HeyGen queue cleanup: use the node script in session history to delete all but last N videos

---

## 🟡 Phase 6 — Publish Integration (Active)

**`UPLOADPOST_API_KEY` ✅ confirmed in `.env` — ready to test**

> ⚠️ **Avatar V Status:** API/MCP access coming "in the coming months" per HeyGen support (no ETA). **Hybrid workaround now available:** Generate with Avatar IV via API → manually upgrade to Avatar V in HeyGen web console → click 🔄 REFRESH IDs button in dashboard to sync upgraded URLs → continue automated pipeline (ASSEMBLE → Gate 3 → Publish). Phase 6 testing can proceed with this workflow.

| Task | Status | Notes |
|------|--------|-------|
| `UPLOADPOST_API_KEY` in `.env` | ✅ Done | JWT token confirmed present |
| HeyGen Avatar V access | 🟡 Waiting | API not yet available — when ready, just swap `HEYGEN_AVATAR_ID` + `HEYGEN_AVATAR_SHORT_ID` in `.env` (zero code changes) |
| Test Case #1: Twitch Long → YouTube Private | 🟡 Ready to run | Spec in `UPLOAD_API_SPEC.md` — Avatar IV works fine, no need to wait for V |
| Test Case #2-5: Multi-platform | ⏳ After Test #1 passes | TikTok + Instagram + simultaneous |
| Approve overnight queue (Aider) | ⏳ Waiting on Rob | See `OVERNIGHT_TASKS.md` |
| Push commits to origin/main | ✅ Done | Pushed `e269afc` — GitHub synced |

---

## 🔧 Tech Debt (Low Priority)

1. No request throttling — could overwhelm Puppeteer under load
2. Asset path hardcoding — should use environment variables
3. QA recorder NBA endpoint — should use `/generate-thumbnail` with `contentType: 'nba'`
4. 12-test suite re-run pending — Tests 2 & 4 fixed, need confirmed 12/12 pass
5. **Audit trail for rollback/force-advance** — neither dashboard log nor `logs/errors.jsonl` records rollback/advance events. Dashboard should append to job card log; server should write `{level:'warn', kind:'rollback'|'advance', jobId, before, after, at}` to `logs/errors.jsonl`. See `ROLLBACK_FORCE_ADVANCE_SPEC.md` → "Audit trail" section.
6. **Revisit Instagram publish strategy after 10 successful Reels** — Upload-Post has no IG draft/private API parameter (confirmed 2026-04-10). Current policy: IG account stays account-wide private until 10 Reels shipped end-to-end with no content issues. After that, run the Trial Reels API test (documented in `ROLLBACK_FORCE_ADVANCE_SPEC.md` → "Publish-Time Privacy" section) to determine whether `share_mode=TRIAL_REELS_DONT_SHARE_TO_FOLLOWERS` actually works on a new account without the 1,000-follower minimum. Based on outcome: (a) wire it into `server.js:7030-7033` and flip account public, (b) drop IG from default `platforms` array until follower count grows, or (c) keep account private indefinitely. YouTube (`private`) + TikTok (`SELF_ONLY`) are already handled correctly at the API level.

---

## 🏗️ Services & Ports

| Service | Port | Command |
|---------|------|---------|
| Dashboard | 8765 | `python3 -m http.server 8765` |
| Node API | 3000 | `nodemon server.js` |
| VectCut API | 9001 | `cd VectCutAPI && ./venv-capcut/bin/python3 capcut_server.py` |

**Dashboard:** http://localhost:8765/cwn_production.html

---

## 🎨 Brand Standards (Quick Reference)

- **CWN Gold:** `#c7af4f`
- **Border:** `5px solid` + `0 4px 15px rgba(0,0,0,0.5)` shadow at 50% opacity
- **Logo Opacity:** 85% (0.85)
- **Long-form Logo:** 120px at `20:20` (top-LEFT, 20px margins — moved from top-right to avoid collision with TV card at x=1240)
- **Short-form Logo:** 80px at `W-w-15:15` (top-right, 15px margins)
- **Tagline:** "BECAUSE THE LIGHT WAS ON" (all thumbnails)

---

## 📁 Key Files

| File | Purpose |
|------|---------|
| `CLAUDE.md` | Full architecture, rules, gotchas (READ FIRST every session) |
| `STATUS.md` | This file — current tasks + system state |
| `QA_GATES.md` | All QA gates (1-6) documented |
| `COMMIT_CHECKLIST.md` | Pre-commit rules for Aider/Cline |
| `server.js` | Node.js API (6000+ lines, all endpoints) |
| `cwn_production.html` | Dashboard UI |
| `data/streamers.json` | Streamer roster + display names + phonetics |
| `data/cwn_style_guides.json` | Gemini-learned style fingerprints per content type |
| `test/test_suite_12cases.json` | 12-test validation suite |
| `data/episode_counters.json` | Episode tracking (twitch/nba/news) |
| `.aider.conf.yml` | Aider config (claude-sonnet-4-20250514) |
| `VISUAL_DESIGN_SPEC.md` | Short-form layout spec (1080×1920, zones, safety) |
| `CREATIVE_VS_OPERATIONS.md` | 7 creative decisions needed for Phase 5 |
| `MORNING_BRIEFING.md` | What Aider did overnight — read every morning before touching code |
| `OVERNIGHT_TASKS.md` | Aider's overnight task queue (1am-7am ET window) |
| `SERVER_SPLIT_PLAN.md` | Plan to split server.js into modules — IN PROGRESS (Phase 1 done: config, logger, metrics extracted ✅) |

---

## 📜 Recent Commits

```
64c2f70  fix: flatten 2D clipAnalyses in claudeScriptQA — correct streamer attribution for Twitch (Fix #3)
186ea3d  Phase 3: generateShortFormCaption + prioritizeNewsStories
b40ee6a  fix: correct Twitch longform tagline to TALK SOUP + switch Aider to Anthropic API
2c0878a  docs: update MASTER_TASK_LIST.md — Phase 2 + Phase 4 marked complete
9fa9340  feat(phase4): Gate 3 human approval + Gate 6 auto-publish wiring
88e20eb  Phase 2: Short-form split-screen + CapCut integration
93aa22f  fix: Scene header normalization (spaces → underscores in Gemini output)
bc3f3a9  Fix Tests 2 & 4: scene header spaces, ExtraEmily displayName, manual v2.1
```

---

## 🗑️ Files to Archive (Pending Claude Notification)

These files are superseded by `STATUS.md` and `CLAUDE.md`. Do not delete until Claude Code is notified:

**Replaced by STATUS.md:**
- `TODO.md` — backlog (now in STATUS.md)
- `AUTOMATION_STATUS.md` — completed items (now in STATUS.md)
- `HANDOVER.md` — session handover (now in STATUS.md)
- `SESSION_SUMMARY.md` — session history (now in STATUS.md)
- `MASTER_TASK_LIST.md` — phase tracker (now in STATUS.md)

**Stale planning docs (Apr 6, superseded by CLAUDE.md):**
- `WORKFLOW_GAPS_ANALYSIS.md`
- `COMPREHENSIVE_REVIEW.md`
- `CODEBASE_OVERVIEW.md`
- `IMPLEMENTATION_PLAN.md`
- `IMPLEMENTATION_SPEC.md`
- `IMPLEMENTATION_AUTOMATION.md`
- `NEXT_TEST_PLAN.md`

**Resolved bug docs:**
- `FIX_PLAN_SCENE_STRUCTURE.md`
- `TEST_FAILURE_ANALYSIS.md`
- `TEST1_POSTMORTEM.md`
- `URGENT_TEST_FAILURE_INVESTIGATION.md`
- `TEST_RESULTS_FINAL_2026-04-09.md`
- `NEWS_COMPILATION_FIX.md`
- `DESIGN_REVIEW_SUMMARY.md`
- `CANVA_FIX_SUMMARY.md`
- `GATE1_MAX_DATA_LIMITS.md`
