# CWN Production — Status & Task Tracker

**Last Updated:** 2026-04-14 (7:20 PM ET)
**Branch:** main | **Latest Commit:** fix(news): implement smoke test bug fixes 1+2 + Gate 3 pre-flight check
**🚨 NEW ARCHITECTURE DOC:** Every agent must read `GATED_PIPELINE_ARCHITECTURE.md` at the start of every session. It supersedes the ad-hoc retry logic patchwork and defines 9 principles + 7 gates + Gate Output Contract + collaborative QA dialogue pattern.
**How to start a session:** Tell Cline: _"Read CLAUDE.md and STATUS.md and tell me what we're working on"_
**Every morning:** `cat MORNING_BRIEFING.md` — see what Aider did overnight before touching anything

---

## 🗂️ Agent Assignments

| Agent | Role | Best For |
|-------|------|----------|
| **Claude Code** | Creative Director + Architect | HTML template design, brand decisions, spec writing |
| **Cline** | Implementation Lead | server.js edits, API integration, pipeline wiring |
| **Aider** | Surgical Coder | Text generation logic, prompt engineering, keyword detection |

---

## 🔒 Active File Locks

> **Two agents running simultaneously? Declare your file lock here before editing. Check this table before opening any Tier 1/2 file. See `AGENT_FILE_REGISTRY.md` for full protocol.**

| File | Agent | Handoff | Locked At |
|------|-------|---------|-----------|
| server.js | Cline-A | CLINE_HANDOFF_SMOKE_TEST_BUGS.md + CLINE_HANDOFF_QA_GATE_HARDENING.md | 2026-04-14 6:03 PM ET |

---

## 🤖 Last Agent Action

> **Every agent must update this table before committing code. The pre-commit hook will block commits that skip this.**

| Agent | Task Completed | Files Changed | Commit | Timestamp |
|-------|---------------|---------------|--------|-----------|
| Claude Code | **docs(assembly): verify News source clip zoom-to-fill is correct (Commit 4 of QA hardening)** — Verified source_clip vfFilter at server.js:4594 uses zoom-to-fill (increase+crop pattern) — Red 4 Fix 4 already correct. `decrease,pad` at 4593 is for avatar segs only (correct). Comment updated to confirm verification. No functional code change. All 4 QA hardening commits complete. | server.js, STATUS.md | — | 2026-04-14 ET |
| Claude Code | **fix(gate3): add News TV card scene-context check to Gemini QA prompt (Commit 3 of QA hardening)** — EARLY TV CARD checklist item now specifies card is only correct on STORY_INTRO scenes for News; visible on non-intro → FAIL. qaPrompt context line adds News chrome rule to all 3 sample prompts. tvCardOnWrongScene: news && /TV CARD.*FAIL/i → hasCriticalFail=true. 15-point deduction added. Why-doc critical failures section updated. | server.js, STATUS.md | — | 2026-04-14 ET |
| Claude Code | **fix(gate3): pass downloadedClipCount into geminiQACheck (Commit 2 of QA hardening)** — Updated geminiQACheck signature to accept downloadedClipCount. MIDDLE sample SOURCE CLIPS item now uses `downloadedClipCount ?? clipCount`. Gemini context line shows "X requested, Y downloaded". clipsExpectedButMissing: hard-fails when clipCount>0 AND effectiveClipCount===0. Gate 3 call site at server.js:5143 now passes downloadedClipCount. Two-layer defense with pre-flight from Commit 1. | server.js, STATUS.md | — | 2026-04-14 ET |
| Cline | **fix(news): implement smoke test bug fixes 1+2 + Gate 3 pre-flight check** — Fix 1: Added News HLS re-scrape in assembly download loop (server.js:3724-3740) to refresh Brightcove fastly_token that expires before assembly runs. Fix 2: Replaced 500ms setTimeout with document.fonts.ready + 100ms timeout in generateNewscastOverlay() (server.js:11474-11538). Also added Gate 3 pre-flight deterministic structural check (server.js:4048-4118) that catches 0-source-clip episodes before Gemini upload. | server.js | 4fc42ed | 2026-04-14 7:16 PM ET |
| Claude Code | **Diagnosed thumbnail gap, wrote CLINE_HANDOFF_THUMBNAIL_WIRE.md** — `thumbDriveUrl` (auto-extracted 15s frame, uploaded to Drive) is only stored in `assemblyJobs[asmId]` (in-memory) and never persisted to `persistedJobs[assemblyJobId]` / `data/jobs.json`. Manual approval path (`approveAndUpload()` in dashboard) sends `thumbnailUrl: prep.thumbnailUrl \|\| job.thumbnailUrl \|\| ''` — both always empty. Auto-publish path (Gate 3 auto-pass) works correctly. Fix: (1) server.js after `thumbDriveUrl` Drive upload, call `saveJobCard(assemblyJobId, { ...persistedJobs[assemblyJobId], thumbDriveUrl })` to persist; (2) dashboard `approveAndUpload()` add `job.canvaUrl \|\| job.thumbDriveUrl` to fallback chain before blank string. S-size, 1 commit. | CLINE_HANDOFF_THUMBNAIL_WIRE.md (new), STATUS.md | — | 2026-04-14 ET |
| Claude Code | **Wrote CLINE_HANDOFF_QA_GATE_HARDENING.md — Gate 3 two structural blindspots** — (1) clipCount counted from segsToProcess (requested), not localFiles (downloaded) — clipCount=0 caused SOURCE CLIPS check to never be asked; fix: deterministic pre-flight check using segTypes+localFiles, plus downloadedClipCount into geminiQACheck. (2) TV card scene-context gap: Gate 3 only checked presence/absence, never asked whether TV card belongs on that scene type; fix: scene-type rule in qaPrompt + tvCardOnWrongScene critical failure. (3) Clip sizing verification: zoom-to-fill already in place (Red 4 Fix 4), likely comment-only. 4 commits, all server.js. | CLINE_HANDOFF_QA_GATE_HARDENING.md (new), STATUS.md | — | 2026-04-14 ET |
| Claude Code | **Diagnosed 3 smoke test bugs, wrote CLINE_HANDOFF_SMOKE_TEST_BUGS.md** — Root cause analysis for (1) 0 source clips: Brightcove fastly_token expires before assembly runs; fix is re-scrape article URL at download time. (2) TV card persists: document.fonts.ready fix from 9b6a403 was not actually applied; 500ms setTimeout still at server.js:11478. (3) Double pronunciation: Gemini prompt issue, Gate 1 QA deduction fix. Handoff ready for Cline-A. | CLINE_HANDOFF_SMOKE_TEST_BUGS.md (new), STATUS.md | — | 2026-04-14 ET |
| Cline | **fix(news): add storyIndex + fix source_clip schema in Gemini News prompt** — Fixed Zod validation errors in Gemini News prompt JSON example at server.js:7623-7650. Added missing storyIndex field to scene example (required Zod field). Fixed source_clip scene example — source_clip scenes must NOT have spokenText field, need clipUrl instead. Added separate source_clip example below avatar example. | server.js | 7638a5c | 2026-04-14 ET |
| Cline | **fix(news): chrome fixes 2-6 + Wave 0 gaps 5, 15, 33** — Fixes 2-6 from CLINE_HANDOFF_NEWS_CHROME_FIX.md: server.js: upgrade directive sidecar catch to console.error (Fix 2); verify Gemini News prompt Zod fields (Fix 3); tools/clipzworld_newscast.html: clear lt-headline DOM placeholder (Fix 4); server.js: replace 500ms setTimeout with document.fonts.ready (Fix 5); server.js: make page.evaluate async, await TV card image load (Fix 6). Wave 0 gaps: server.js: remove dead /news/generate-intro-card endpoint (Gap #5); server.js: humanize YouTube chapter labels for News/NBA/Twitch (Gap #15+#33). | server.js, tools/clipzworld_newscast.html | 9b6a403 | 2026-04-14 ET |
| Claude Code | **fix(news/pipeline): wire directive sidecar jobId to assembly, fix isClipMatchOnly, delete dead services/** — Fix 1 of 6 from CLINE_HANDOFF_NEWS_CHROME_FIX.md: cwn_production.html now stores metricsJobId as CURRENT_META.scriptJobId and includes jobId in assembly payload so hasDirectiveForJob() finds the directive sidecar (root cause of missing TV card/flag/sidebar on every News run). isClipMatchOnly logic fixed to use .some(d => d.type !== 'clip_match') so surgical Gate 1 fix path is reachable. services/assembly.js, services/qa.js, services/script_generation.js deleted (dead code, all functions duplicated in server.js). | cwn_production.html, server.js, services/ (deleted) | pending | 2026-04-14 ET |
| Cline | **refactor: code review cleanup — remove unused imports, fix puppeteer crash, hoist cheerio** — Comprehensive code review (19 findings in CODE_REVIEW.md). Safe fixes applied: (1) Removed 4 unused imports: validateChromeScript, withRetry, getFallbackImage, validateUrl. (2) Added execSync to top-level child_process destructure. (3) Fixed C-3 CRITICAL runtime crash: puppeteer.newPage() → browser.newPage() in /news/generate-intro-card route. (4) Improved Gate 6 error logging: now captures e.response?.data (HTTP response body) instead of just e.message. (5) Hoisted cheerio to top-level require (line 100) and removed 3 inline re-requires. Server restarts cleanly. node -c server.js → exit 0. | server.js, CODE_REVIEW.md (new), STATUS.md | pending | 2026-04-14 4:10 AM ET |
| Cline | **fix(news): send scriptForHeygen (plain text) to dashboard, not raw JSON directive** — Root cause of Gate 2 `Expected 2 segments, got 0`: `res.json({ script, ... })` in `/generate-full-script` was returning the raw Gemini JSON directive for News jobs instead of the extracted plain spoken text. Dashboard textarea received JSON → `parseSegments_v2` found 0 `=== HEADER ===` markers → Gate 2 reported `segment_count_mismatch: Expected 2 segments, got 0`. Fix: `res.json({ script: scriptForHeygen, ... })` — `scriptForHeygen` is the plain text extracted by `extractSpokenTextFromDirective()` for News directive mode, or the raw script unchanged for all other content types (Twitch/NBA). Backward-compatible. `node -c server.js` → OK. | `server.js`, `STATUS.md` | pending | 2026-04-14 3:28 AM ET |
| Cline | **Red 4 chrome bug fixes — 4 bugs from asm_1776140626023**: Fix 1 (prev session): Gemini prompt chrome format block rewritten to match ScriptSchema exactly — eliminates schema drift. Fix 2 (prev session): `validateChromeScript()` now called after JSON.parse in assembly path — schema drift is no longer silent. Fix 3a (prev session): `directiveToOverlayParams()` now exposes `tvCard` in return object. Fix 3b (prev session): `generateChromeOverlayFromDirective()` destructures and passes `tvCard` through. Fix 3c (prev session): `generateNewscastOverlay()` accepts `tvCard = null` in options; `page.evaluate()` passes `tvCard` and includes TV card injection code (show/hide `.tv-card`, set `.tv-card-image` src, `.tv-card-headline` text, `.tv-card-source` text). Fix 3d (this session): Added `.tv-card` div to `tools/clipzworld_newscast.html` — CSS positioned at OVERLAY_ZONE (absolute, left:1240px, top:40px, 520×293, navy bg, gold border, hidden by default) + HTML with `.tv-card-image`, `.tv-card-meta`, `.tv-card-headline`, `.tv-card-source` child elements. Fix 4 (prev session): Source clip vfFilter changed from `decrease,pad` (letterbox) to `increase,crop` (zoom-to-fill) — eliminates portrait-with-navy-bars on Al Jazeera clips. `node -c server.js` → exit 0. | `server.js`, `lib/chromeDirectives.js`, `tools/clipzworld_newscast.html`, `STATUS.md` | pending | 2026-04-14 1:25 AM ET |